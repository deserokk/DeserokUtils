<?xml version="1.0"?>
<doc>
    <assembly>
        <name>DeserokUtils</name>
    </assembly>
    <members>
        <member name="T:DeserokUtils.DebuffMark">
            <summary>
            One status worth marking over somebody's head.
            </summary>
        </member>
        <member name="P:DeserokUtils.DebuffMark.Status">
            <summary>The name as typed, kept so the box can show it back and the id can be re-resolved.</summary>
        </member>
        <member name="P:DeserokUtils.DebuffMark.MineOnly">
            <summary>
            ⚠⚠ ON by default, and the default matters. Kuzushi, the status this feature was built for, reads
            *"damage taken from the samurai who applied this effect"* -- so somebody else's copy of the same
            status is the WRONG answer. Defaulting this off would produce a marker that is confidently wrong
            exactly when a limit break is spent on it.
            </summary>
        </member>
        <member name="T:DeserokUtils.MarkOverride">
            <summary>
            A glyph chosen for one specific character. See <see cref="P:DeserokUtils.Configuration.MarksOverrides"/> for the
            limits on what this is allowed to influence.
            </summary>
        </member>
        <member name="P:DeserokUtils.MarkOverride.Who">
            <summary>⚠ <c>Name@World</c> as typed. Matched case-insensitively; never parsed into an id.</summary>
        </member>
        <member name="P:DeserokUtils.MarkOverride.Glyph">
            <summary>Font Awesome codepoint, used when <see cref="P:DeserokUtils.MarkOverride.Shape"/> is Icon. Defaults to a heart,
            which is the reason this whole feature exists.</summary>
        </member>
        <member name="P:DeserokUtils.MarkOverride.Colour">
             <summary>
             This person's own colour, or null to use the shared marker colour.
            
             ⭐ NULLABLE RATHER THAN ALWAYS SET, deliberately. If every override carried a colour, it would
             be seeded from the shared one at the moment it was created and then silently stop tracking it
             -- so changing the marker colour later would move everybody except the people you had
             customised, which is the opposite of what "I only changed their shape" implies.
             </summary>
        </member>
        <member name="T:DeserokUtils.FateRotation">
             <summary>
             One ring of FATEs that take turns, in one territory.
            
             ⚠⚠ THE TERRITORY IS PART OF THE ROTATION, not a filter applied to it. The Occult Crescent has two
             zones with two pot FATEs each, and they are different FATEs on independent rings -- South Horn
             runs Persistent Pots / Pleading Pots, North Horn runs Daylight Pottery / In a Pot of Bother.
             A single flat list cannot express that: it would make the ring four long and halve every
             prediction in both zones.
            
             ⭐ This is the split DeserokUtils.md predicted and asked for -- "what I track" and "what is in
             the rotation" were two ideas sharing one list, and the note said to separate them the moment
             there were two rotations. There are now two.
             </summary>
        </member>
        <member name="P:DeserokUtils.FateRotation.Zone">
            <summary>Human name for the zone, for diagnostics only. Nothing keys off it.</summary>
        </member>
        <member name="P:DeserokUtils.FateRotation.Members">
            <summary>Ring members IN ORDER. The order is which one comes next, so it is load-bearing.</summary>
        </member>
        <member name="P:DeserokUtils.FateRotation.Labels">
             <summary>
             Short label per member for the server bar.
            
             ⚠⚠ NAME COLLISION, and it is a nasty one now: North Horn's labels are "N" and "S" for the
             NORTH and SOUTH ends OF THAT ZONE -- they have nothing to do with the zones being called
             North Horn and South Horn. A bar reading "36m S" in North Horn means "south side of this
             map", not "South Horn". Rename them the moment that reads wrong to anybody.
             </summary>
        </member>
        <member name="P:DeserokUtils.FateRotation.SlotMinutes">
             <summary>
             Minutes between consecutive SLOTS -- one pot to the next pot, whichever it turns out to be.
            
             ⚠⚠ NOT the gap between two spawns of the same FATE, which is this times <see cref="P:DeserokUtils.FateRotation.Members"/>
             count. Both are "the cycle" in English and mixing them doubles or halves every prediction.
             </summary>
        </member>
        <member name="T:DeserokUtils.Configuration">
             <summary>
             Persisted settings. Dalamud stores this as JSON next to the plugin.
            
             ⚠ Observed spawn times live here on purpose: a prediction is only as good as its anchor, and an
             anchor that dies on logout means the first half hour of every session is blind.
             </summary>
        </member>
        <member name="P:DeserokUtils.Configuration.Version">
            <summary>
            ⚠ A default only applies to a config that does not exist yet. Once one is on disk, changing
            a default here is INERT -- the migration below is what actually reaches an existing install.
            </summary>
        </member>
        <member name="F:DeserokUtils.Configuration.ReplaceList">
             <summary>
             ⚠⚠ EVERY LIST IN THIS FILE MUST CARRY THIS, AND HERE IS WHY.
            
             Newtonsoft defaults to ObjectCreationHandling.Auto: when a property's getter already returns
             a non-null collection -- which every `= new() {...}` initialiser below guarantees -- it does
             not replace that collection, it ADDS the JSON items to it. So each load produced the defaults
             PLUS everything on disk, and each save wrote the result back. The lists grew by their own
             length on every single plugin load, silently, forever.
            
             Measured 2026-08-07: "Kugane" was in FcBuffSafePlaces 21 times, and AlertMinutes read
             [10, 5, 10, 5, 10, 5, ...] on a shipped install that had never been edited.
            
             ⭐⭐ THIS IS WHERE THE FATEWATCH DUPLICATES CAME FROM. TrackedFates was the one list that
             looked healthy, and only because Migrate() dedupes it on every load -- a fix written for the
             symptom while the cause kept running underneath it, in every other list. The 117.7-minute
             prediction that "did not quite land" as 30 x 4 = 120 was this: not four tracked FATEs, but a
             measured value doubled by a list that had silently doubled.
            
             ⚠ Replace makes the deserialiser overwrite the initialiser instead of appending to it. The
             defaults still apply to a config that does not exist yet, which is the only thing they were
             ever for.
             </summary>
        </member>
        <member name="P:DeserokUtils.Configuration.Verbose">
             <summary>
             Diagnostic output to the Debug chat channel.
            
             ⚠ OFF by default. It shipped on because everything was being diagnosed, and CastWatch logs a
             line per UseAction while armed -- which against a thirteen-line fallback macro is thirteen
             lines per press. Correct while hunting a bug, noise once the bug is dead.
             </summary>
        </member>
        <member name="P:DeserokUtils.Configuration.Rotations">
             <summary>
             Every FATE ring the plugin knows about, one per territory.
            
             ⚠⚠ These FATEs ALTERNATE within their zone. The 30-minute figure is the gap between
             *consecutive members*, so each individual one recurs every 60 minutes. Treating them as two
             independent 30-minute timers predicts each at exactly the moment the OTHER is due --
             confidently, and wrong every single time.
            
             ⭐ North Horn confirmed from the live FATE table 2026-08-03: Daylight Pottery (north end) at
             21:31:35, In a Pot of Bother (south end) at 22:01:39 -- thirty minutes four seconds apart.
            
             ⚠⚠ South Horn was in this plugin ONCE AND WAS DELETED. v2's migration removed "Persistent
             Pots" and "Pleading Pots" as wiki guesses that were "different FATEs entirely". They were
             real -- ids 1976 and 1977, confirmed from the Fate sheet 2026-08-12 -- and the wiki was
             describing the OTHER ZONE. The migration's guard even asked the right question ("has this
             ever spawned?") and got the locally-true, globally-wrong answer, because at that point only
             North Horn had ever been visited. Absence of evidence from one territory.
            
             ⚠ South Horn's labels are empty because nobody has recorded where its two pots spawn yet.
             Empty labels render fine; fill them in once observed.
             </summary>
        </member>
        <member name="P:DeserokUtils.Configuration.AlertMinutes">
            <summary>Minutes before a predicted spawn to warn at. Descending.</summary>
        </member>
        <member name="P:DeserokUtils.Configuration.LastSeen">
            <summary>Last observed spawn per FATE name, as unix seconds.</summary>
        </member>
        <member name="P:DeserokUtils.Configuration.MeasuredIntervals">
             <summary>
             Gaps between consecutive observed spawns OF THE SAME FATE, in minutes. Kept rather than
             averaged away so an outlier is visible as an outlier instead of quietly dragging the mean.
            
             ⚠⚠ SAME-FATE gaps, so ~60 -- a different unit from a rotation's SlotMinutes, which is ~30.
             It has to be: RecordSpawn only ever sees one name twice, so a slot gap is not observable
             here. Divide by the rotation length before using it as a slot -- FateTracker.EffectiveCycle
             is the single place that conversion lives.
            
             ⭐ Keyed by FATE NAME, not by rotation, and that is why the rotation refactor could throw the
             old lists away without losing anything: a measured interval is evidence about a FATE, and
             stays true no matter how the rings are arranged around it.
             </summary>
        </member>
        <member name="P:DeserokUtils.Configuration.LastSeenTerritory">
            <summary>Territory each FATE was last seen in, so the bar can hide itself elsewhere.</summary>
        </member>
        <member name="P:DeserokUtils.Configuration.LastSeenInstance">
             <summary>
             Zone instance each FATE was last seen in, paired with <see cref="P:DeserokUtils.Configuration.LastSeenTerritory"/> to say
             exactly WHERE an anchor was made -- which is what makes it possible to notice it no longer
             applies.
            
             ⚠ A second dictionary rather than widening LastSeenTerritory's value type, for the same reason
             FateLabels is its own dictionary: changing the shape of something already on disk loses what
             is in it, and the spawn anchors are the expensive thing to lose.
            
             ⚠ An anchor written before this field existed has no entry here. Missing is treated as
             "matches", never as stale -- otherwise updating the plugin while standing in the zone would
             throw away a perfectly good anchor on the first tick. No migration needed for the same reason.
             </summary>
        </member>
        <member name="P:DeserokUtils.Configuration.FcBuffsEnabled">
             <summary>
             ⚠ OFF, and it costs nothing to leave off. A NEW key absent from an existing config keeps
             whatever this initialiser sets -- which is the opposite of the trap documented above, where
             changing the default on an EXISTING key reaches nobody. Additions are free; edits need
             <see cref="M:DeserokUtils.Configuration.Migrate"/>. Both halves of that rule are worth knowing, because only one of them
             has ever caused a bug here.
            
             ⚠⚠ And this one is automation: it acts without being asked, which nothing else in this plugin
             does. Defaulting it on would switch that behaviour on for an existing install silently.
             </summary>
        </member>
        <member name="P:DeserokUtils.Configuration.FcBuffsDryRun">
             <summary>
             Go through every step EXCEPT the activation, and log what would have been pressed.
            
             ⚠ OFF as of v1.5.1. It shipped ON in 1.5.0 as a first-release guard, and the guard did its
             job -- the dry-run log is what confirmed the row index before anything was consumed. Once the
             chain was verified end to end, leaving it on meant a fresh install would tick its buffs,
             enable the feature, and silently do nothing until it found this checkbox.
            
             ⚠⚠ NO MIGRATION, on purpose, and this is the rule rather than an oversight: a default is for a
             config that does not exist yet. Forcing it off in Migrate() would override anyone who had
             deliberately turned it ON -- which is exactly the "rehearse before you trust it" case this
             setting exists to serve.
             </summary>
        </member>
        <member name="P:DeserokUtils.Configuration.FcBuffActions">
             <summary>
             Company action names to keep running, matched against the CompanyAction sheet.
            
             ⚠ Empty on purpose. The two that get used are Heat of Battle and Reduced Rates, but their
             exact row names carry roman numerals and this is not the place to guess at one -- the tab
             lists the real names out of the game and they get ticked there.
             </summary>
        </member>
        <member name="P:DeserokUtils.Configuration.FcBuffSafePlaces">
             <summary>
             Where acting is allowed: cities and residential districts, as PLACE NAMES.
            
             ⭐ Names rather than territory ids. Cities are finite and known, so the list can simply be
             written down -- but a column of numbers is unverifiable by eye and silently wrong forever,
             whereas a name that does not resolve says so in the tab. The sheet is the authority for the
             number; the name is the part a human can check.
            
             ⚠ Seeded from memory, so treat a name that fails to resolve as a typo here rather than as a
             missing zone -- and note this list goes stale on the next expansion, which is precisely when
             it will be needed. /fcbuffs here adds wherever you are standing without editing anything.
             </summary>
        </member>
        <member name="P:DeserokUtils.Configuration.FcBuffLowStockWarning">
             <summary>
             Warn when the stock of a wanted buff drops to this or below.
            
             ⚠ This is the constraint the credit floor was mistakenly standing in for. Automatic
             activation quietly consumes stock, and the failure mode when it runs out is the plugin going
             silently back to doing nothing -- which is indistinguishable from it working, right up until
             the eight-hour grind it was built to prevent.
             </summary>
        </member>
        <member name="P:DeserokUtils.Configuration.FcBuffCheckSeconds">
             <summary>
             Seconds between checks of whether the buffs are still up.
            
             ⭐ 60 by default, and it could honestly be 300 -- these buffs last 24 hours, so any interval
             short of "hours" is over-sampling. The only thing granularity buys is how quickly the "it
             just ran out" toast reaches you while you are in the field, and a minute is already far
             inside the timescale of the problem this exists to solve.
            
             ⚠ The check itself is now id lookups against the status list, so the interval is a comfort
             setting rather than a performance one. Slowing a cheap thing down is not where the cost was.
             </summary>
        </member>
        <member name="F:DeserokUtils.Configuration.DefaultDrawCommand">
             <summary>
             ⚠ The literal command line, not an emote name. The feature sends it verbatim through the
             chatbox, so whatever works when typed works here.
            
             ⚠⚠ "motion" is a SUBCOMMAND, and the game's own usage text writes it as `/draw [subcommand]`
             -- the square brackets mean "optional" and are not typed. `/draw [motion]` is a different
             string from `/draw motion`, and the difference is whether the chat line goes out with it.
             </summary>
        </member>
        <member name="F:DeserokUtils.Configuration.DefaultSheatheCommand">
            <inheritdoc cref="F:DeserokUtils.Configuration.DefaultDrawCommand"/>
        </member>
        <member name="P:DeserokUtils.Configuration.DrawCommand">
             <summary>
             Sent when the weapon is AWAY. See <see cref="F:DeserokUtils.Configuration.DefaultDrawCommand"/>.
            
             ⭐ Configurable for one reason only: it costs a string and it outlives this specific Gold
             Saucer purchase. The Emote sheet currently holds exactly two draw/sheathe rows (237 and 238),
             so there is nothing else to point it at today -- but "which emote" is not a thing the toggle
             logic should have an opinion about.
            
             ⚠ A NEW key, so no migration: absent from an existing config, it keeps this initialiser.
             Additions are free; only edits to an existing key need <see cref="M:DeserokUtils.Configuration.Migrate"/>.
             </summary>
        </member>
        <member name="P:DeserokUtils.Configuration.SheatheCommand">
            <summary>Sent when the weapon is OUT. See <see cref="P:DeserokUtils.Configuration.DrawCommand"/>.</summary>
        </member>
        <member name="P:DeserokUtils.Configuration.DrawSheatheRepeatCollapseMs">
             <summary>
             Presses closer together than this many milliseconds are treated as ONE press. 0 disables.
            
             ⚠⚠ AN ACCESSIBILITY SETTING, NOT A TUNING DETAIL. deserok uses a key repeater as an assistive
             device because tapping keys is difficult. It works like ordinary auto-repeat -- a tap is one
             press, holding past about half a second streams presses at ~10 Hz (102 ms apart, measured
             2026-08-17) until release. A normal tap is therefore unaffected by this setting; the hold is
             what produced a dozen weapon toggles a second.
            
             ⚠⚠ And a cooldown alone does NOT solve the hold, which is the trap: gating on the game's
             one-second sheathe cooldown turns a dozen toggles into one per second and keeps going for as
             long as the key is down. Slower cycling still reads as broken. Only a quiet-gap test collapses
             a held key to a single action.
            
             ⭐ 250 is better than 2x the measured 102 ms repeat, and comfortably below the repeater's own
             ~500 ms hold-to-repeat delay, so it cannot swallow a deliberate second tap. It lives here
             rather than in a constant because repeat rates are a property of somebody's hardware, and a
             person whose device repeats slower should be able to say so without a rebuild.
             </summary>
        </member>
        <member name="P:DeserokUtils.Configuration.UseDefaultToggleWhenEmoteWouldFail">
             <summary>
             When the emote would be refused, use the game's own draw/sheathe instead.
            
             ⚠⚠ ON, because the emote SIMPLY DOES NOTHING in those cases -- refused with no message, so
             without this the key is dead exactly when you are running or jumping, which is the failure
             the old macro had. Off is the pre-existing emote-only behaviour.
            
             ⚠ Named for the CONDITION, not for movement, and that rename is not cosmetic. It shipped for
             an evening as UseDefaultToggleWhileMoving, then jumping turned out to refuse the emote too
             while reading IsPlayerMoving == false. A setting named after one case invites the next one to
             be bolted on as a second setting; named after the reason, it just grows a new entry in
             DrawSheatheFeature.EmoteRefusedBecause.
            
             ⭐ Kept as a switch rather than hardcoded for one specific reason: this branch is the only
             part of the feature that calls into the client directly (UIState.WeaponState.SetUnsheathed)
             rather than sending a text command, so it is the only part a patch can break outright.
             Turning it off restores a working key without waiting on a build -- which is the whole reason
             this plugin exists instead of a dependency on somebody else's.
             </summary>
        </member>
        <member name="P:DeserokUtils.Configuration.EmoteQuietEnabled">
             <summary>
             Suppress the chat log message on a repeat of an emote you have already announced.
            
             ⚠⚠ OFF by default even though it was asked for, because it changes what OTHER PEOPLE see.
             Every other setting in this file affects only deserok's own client; this one edits what
             reaches strangers' chat logs, and a plugin that starts doing that on install without being
             asked is the wrong shape. It costs one checkbox, once.
            
             ⚠⚠ It does nothing unless the game's own "Display log message" is TICKED in the emote window.
             That checkbox is the thing this exists to let you finally leave on -- with it off there is no
             message to suppress, and the feature would appear broken while working perfectly.
             </summary>
        </member>
        <member name="P:DeserokUtils.Configuration.EmoteQuietWindowSeconds">
             <summary>
             How long after announcing an emote to stay quiet about that same emote, in seconds.
            
             ⚠ PER EMOTE, not global -- clapping must not silence your next /dote. 60 is the figure from
             the original ask: "display the first of a given emote, then hide log messages for the same
             emote within 1 minute".
             </summary>
        </member>
        <member name="P:DeserokUtils.Configuration.EmoteQuietIncomingEnabled">
             <summary>
             Hide OTHER people's repeated emote lines in your own log, same window, keyed per sender.
            
             ⚠⚠ OFF by default, and for a different reason than the outgoing half. That one changes what
             others see; this one deletes things from your own chat log, and a filter that is too broad
             hides its own mistakes by construction. Opt in, then audit it -- the tab counts what it hid.
            
             ⭐ Per sender is not a default to revisit, it is the design: "knowing 10 people clapped is
             nice, knowing 1 guy spammed clap 30 times is noise". Audience size is signal, repetition is
             noise, and keying on sender-plus-line is exactly the line between them.
             </summary>
        </member>
        <member name="P:DeserokUtils.Configuration.EmoteQuietFamilies">
             <summary>
             Name prefixes whose emotes all count as ONE emote for the quiet window.
            
             ⚠⚠ THE CHEER VARIANTS ARE SIXTEEN SEPARATE EMOTES THAT NOBODY TREATS AS SEPARATE. Observed
             behaviour, deserok and others alike: you cycle through them hunting the colour you want and
             then settle on one. Keyed per emote id, that cycle announces every step.
            
             ⭐ Confirmed from the sheet 2026-08-17: fifteen rows begin "Cheer " -- On/Wave/Jump/Rhythm/
             Light across five colours -- and every one of them renders the IDENTICAL log line, "…cheers to
             the rhythm." (their LogMessage rows are byte-for-byte the same). So collapsing them loses
             nothing that was ever visible. The trailing space matters: plain "Cheer" is a different emote
             with a different line ("…lets out a cheer.") and stays on its own.
            
             ⚠ A hand-written list, deliberately, and NOT derived by grouping emotes that share a log
             message. That derivation was tried and is a trap: ExtractText drops the verb -- it is stored as
             a singular/plural pair inside the payload -- so Bow, Clap, Wave, Yes, No, Smile and fifteen
             others all reduce to ".". Grouping on it would silence your next bow because you clapped, and
             do it invisibly. Fifteen names verified by eye beats a clever rule nobody can check.
            
             ⚠ Prefix match on the emote NAME, case-insensitive. Anything not matching is its own family.
             </summary>
        </member>
        <member name="P:DeserokUtils.Configuration.MarksEnabled">
             <summary>
             Mark the people you came into large content with. Client-side only; nobody else sees it.
            
             ⚠ ON by default, unlike FcBuffs and EmoteQuiet. Those act on the world -- one spends stock,
             the other changes what strangers see. This draws a triangle on your own screen and is invisible
             to everyone else, so there is nothing to opt into.
             </summary>
        </member>
        <member name="P:DeserokUtils.Configuration.MarksMaxGroupSize">
             <summary>
             If you came in with more than this many people, mark nobody.
            
             ⭐⭐ THE DEGENERACY GUARD, and deserok's simplification of a fifteen-checkbox content list.
             When the premade IS the whole group -- Chaotic Raid, a static, a premade dungeon -- marking
             everyone is the same as marking nobody. The content this exists for caps premades anyway:
             Frontlines at 4, Crystalline Conflict at 2, and full-8 alliance premades are rare.
            
             ⭐ So 5 clears every legitimate case and excludes content you enter as a whole group -- which
             means **Chaotic Raid needs no special case**. It is excluded by the reason it should be,
             rather than by somebody remembering to type it into a list.
             </summary>
        </member>
        <member name="P:DeserokUtils.Configuration.MarksInPvp">
             <summary>
             ⭐ Three grouped toggles rather than one per content type. `TerritoryIntendedUse` does the
             classification underneath, so a new field operation sorts itself; the reader sees three boxes.
            
             ⚠ Dungeons and normal raids are deliberately absent — four or eight names is easy to read, and
             with one friend you are already half the group.
             </summary>
        </member>
        <member name="P:DeserokUtils.Configuration.MarksInFieldOps">
            <inheritdoc cref="P:DeserokUtils.Configuration.MarksInPvp"/>
        </member>
        <member name="P:DeserokUtils.Configuration.MarksInAllianceRaid">
            <summary>
            24 players who all look alike. ⭐ Included after Bunny argued it is a PvE Frontline and was
            right: nameplate colour separates party from alliance, NOT your friends from the randoms in
            your own party. Cover is the sharpest case -- it can kill the caster, so which ally is under
            the cursor changes whether the risk is worth taking.
            </summary>
        </member>
        <member name="P:DeserokUtils.Configuration.MarksInDeepDungeon">
             <summary>
             Deep dungeons -- Palace of the Dead, Heaven-on-High, Eureka Orthos, Pilgrim's Traverse.
            
             ⚠⚠ THIS ONE IS IN FOR A DIFFERENT REASON THAN THE OTHERS, and the difference is worth keeping
             straight. Every group above answers *which of these identical allies is my friend* -- the
             marker discriminates. A deep dungeon party is four people who are all your friends, so by that
             test it should be excluded. deserok, 2026-08-22, after a Heaven-on-High run: *"we lose each
             other constantly."*
            
             ⭐ The value there is **wayfinding, not identification**. The floors are dark randomised mazes
             and the marker says where somebody IS, which is a separate job the same overlay happens to do.
            
             ⭐ Note the size threshold never fires here and needs no exception: a deep dungeon party caps at
             four, so the snapshot is at most three, always under the limit of five. The guard that exists
             to catch "the premade is the whole group" would have excluded this on principle -- it does not,
             only because the numbers happen to fall the right way. Worth knowing before anyone raises that
             limit.
            
             ⭐ New key, so it reaches existing configs at this default -- additions are free, edits are what
             need a migration.
             </summary>
        </member>
        <member name="P:DeserokUtils.Configuration.DebuffMarksEnabled">
            <summary>Mark anyone carrying a status you are watching. See DebuffMarksFeature.</summary>
        </member>
        <member name="P:DeserokUtils.Configuration.DebuffMarksScale">
             <summary>
             ⚠ 1.0, not the 2.0 EphemeralMarks uses. A solid Font Awesome glyph reads far larger than an
             outlined reticle at the same nominal size -- deserok, on the first skull: *"massive though, 2x
             is a bad default for these."*
            
             ⚠ This reaches new installs only. A changed default cannot touch a config that already exists,
             which is the trap recorded in ORIENTATION -- deserok sets his own back to 1.
             </summary>
        </member>
        <member name="P:DeserokUtils.Configuration.DebuffMarksHeight">
            <inheritdoc cref="P:DeserokUtils.Configuration.MarksHeight"/>
        </member>
        <member name="P:DeserokUtils.Configuration.DebuffMarksLift">
            <inheritdoc cref="P:DeserokUtils.Configuration.MarksLift"/>
        </member>
        <member name="P:DeserokUtils.Configuration.DebuffMarksPreview">
             <summary>
             Draw a marker on YOURSELF, so the height and clearance can be set without hunting for a target.
            
             ⭐ deserok's idea, from having actually configured the other one: *"to configure, we had to go to
             an explorer mode to render the marks, when a simple show on self toggle for configuring would be
             ideal."* Tuning a position against a thing you can only see in specific content is a bad loop.
            
             ⚠ Persisted and defaulting OFF. It is deliberately not auto-cleared when the window closes --
             that would be surprising, and someone may want to walk around checking it.
             </summary>
        </member>
        <member name="P:DeserokUtils.Configuration.MarksPreview">
            <inheritdoc cref="P:DeserokUtils.Configuration.DebuffMarksPreview"/>
        </member>
        <member name="P:DeserokUtils.Configuration.DebuffMarks">
            <summary>
            ⚠ Empty by default, so the feature costs one bool test until somebody actually wants it.
            </summary>
        </member>
        <member name="P:DeserokUtils.Configuration.MarksLeaderShape">
            <summary>
            The glyph for the party leader, and for everyone else. Bunny, #dev-notes 2026-08-22:
            *"Add different symbols for marks please :) Heart is needed^"*
            </summary>
        </member>
        <member name="P:DeserokUtils.Configuration.MarksMemberShape">
            <inheritdoc cref="P:DeserokUtils.Configuration.MarksLeaderShape"/>
        </member>
        <member name="P:DeserokUtils.Configuration.MarksLeaderGlyph">
            <summary>Font Awesome codepoints for the two defaults, used when their shape is Icon.</summary>
        </member>
        <member name="P:DeserokUtils.Configuration.MarksMemberGlyph">
            <inheritdoc cref="P:DeserokUtils.Configuration.MarksLeaderGlyph"/>
        </member>
        <member name="P:DeserokUtils.Configuration.MarksOverrides">
             <summary>
             Per-person glyph overrides, keyed <c>Name@World</c>, case-insensitive.
            
             ## ⚠⚠ THIS IS THE ONE THING IN MARKS THAT IS WRITTEN TO DISK. Read before extending it.
            
             Everything else about this feature is deliberately ephemeral -- the snapshot dies when you
             leave, and the design notes say *"nothing is persisted"*. That sentence is now qualified rather
             than true, so it needs to be qualified HERE, where the exception lives.
            
             ⭐ What makes it acceptable is the narrow shape deserok drew, 2026-08-23: *"We don't apply said
             marks all the time, or change how the plugin works, we simply allow a shape override to exist
             when they would already be applied."*
            
             ⚠⚠ So: **an entry here can never cause somebody to be marked.** It only changes the glyph for
             someone already in the snapshot. Wire it anywhere into the decision of WHO gets a marker and it
             becomes the "list of people you care about" that this feature rejected twice on the record --
             bigger, and a roster to maintain.
            
             ⚠ A name and a home world, typed by the user, and nothing else. Never an account or content id.
             </summary>
        </member>
        <member name="P:DeserokUtils.Configuration.MarksShowTag">
             <summary>
             Show a Helldivers-style tag -- first initial plus party slot, "P2" -- under the marker.
            
             ⭐ A NEW KEY replacing MarksShowNames rather than a changed default, on purpose. The name
             default was flipped to false and could not reach an existing config -- exactly the trap this
             file opens with. A new key takes its initialiser; additions are free, edits need a migration.
            
             ⚠ OFF. The shapes carry the signal and a variable-width name jitters as people move; two glyphs
             never do. Worth turning on once there are three friends and only two shapes.
             </summary>
        </member>
        <member name="P:DeserokUtils.Configuration.MarksEverywhere">
             <summary>
             Ignore the content gate entirely. ⚠ FOR TESTING THE MARKER ITSELF -- shape, size, head height
             -- somewhere convenient rather than waiting for a Frontline.
            
             ⚠ Deliberately NOT implemented by adding Trial to the content list. That list says where the
             feature is useful; adding a content type to make testing easy would leave it also saying where
             it was once convenient, and those are different facts that would drift apart.
            
             ⭐ The group-size guard still applies, so this is a gate bypass rather than a "show everything"
             switch.
             </summary>
        </member>
        <member name="P:DeserokUtils.Configuration.MarksHeight">
             <summary>
             How far above a character the marker floats, in yalms.
            
             ⚠ YALMS, not pixels, so it tracks distance the way the nameplate does -- a pixel offset would
             clear the name up close and collide with it far away. Configurable because races differ by a
             lot between Lalafell and Roegadyn, and 2.4 was picked rather than measured.
             </summary>
        </member>
        <member name="P:DeserokUtils.Configuration.MarksLift">
             <summary>
             Extra clearance above the anchor, in SCREEN PIXELS (scaled with the marker).
            
             ⚠⚠ Both are needed and they do different jobs. A world offset shrinks with distance, so on its
             own the marker collapses onto the character exactly when they are far away and small -- landing
             on the nameplate, which is the moment it most needs to be clear. A pixel offset never shrinks.
             deserok, seeing it at range: *"anchoring to head position was bad anyways, because of distance
             causing overlap with the frame."*
             </summary>
        </member>
        <member name="P:DeserokUtils.Configuration.MarksScale">
             <summary>
             Marker size, on top of an automatic scale derived from the viewport height.
            
             ⚠⚠ SIZE IN RAW PIXELS IS WRONG ACROSS SCREENS. The same 26px marker is a larger fraction of a
             1080p screen than a 1440p one -- and the three people who will use this run 1440p large, 1440p
             small, and 1080p. So the drawing divides by viewport height against a 1440 baseline, which makes
             the default already correct on all three, and this multiplier is left for taste.
            
             ⚠ Viewing distance is unknowable, which is why there is a slider at all rather than the scaling
             trying to be clever about physical screen size.
             </summary>
             ⭐ 2.0 by default: what deserok settled on after seeing it in game at 1440p, where the
             automatic viewport scale is exactly 1. New installs start where he ended up rather than at a
             size he already rejected as too small.
        </member>
        <member name="P:DeserokUtils.Configuration.MarksColour">
             <summary>⚠ A persisted Vector4, so it needs no Replace attribute -- that trap is collections.</summary>
             <summary>
             ⚠⚠ NOT yellow, blue or red -- those are the three Frontlines team colours, and a marker that
             matches a team colour is worse than no marker: it reads as faction information, which is the
             one thing you must not misread in PvP. deserok caught this before it shipped.
            
             ⚠ Orange was the suggested alternative and is also avoided, narrowly: it sits between red and
             yellow and is close to Immortal Flames' branding.
            
             ⭐ Magenta instead. It is the colour furthest from all three team colours, no FFXIV UI element
             uses it, and it is not the white of a target reticle -- so it cannot be mistaken for "this is
             my current target" either. Picker in the tab for anyone who disagrees.
             </summary>
        </member>
        <member name="M:DeserokUtils.Configuration.Migrate">
            <summary>
            ⚠⚠ Reaches an EXISTING config, which a changed default cannot.
            </summary>
        </member>
        <member name="T:DeserokUtils.Features.AchievementData.AchievementPreload">
             <summary>
             Ask the server for your completed achievements once per login, so the rest of the game's own
             achievement UI behaves.
            
             ## ⭐⭐ What this replaces, and why the replacement is one call
            
             This started as a full feature: decode achievement chat links, rewrite them, show a tooltip.
             ⚠ All of that is deleted. Something already installed shows an achievement tooltip in the native
             chat window -- not this plugin, and not VanillaPlus, provider unidentified -- so the gap it was
             built for does not exist where it was thought to be.
            
             ⭐ What DID hold up is the diagnosis underneath it. Clicking an achievement link opened "Last Five"
             rather than the achievement, until the Achievements window had been opened once; afterwards it
             navigated correctly. The client cannot look up what it has not loaded, and it only loads on demand.
            
             ⭐ deserok's call: *"why not simply call for achievement data once at the beginning of a session."*
             The game already has the capability and the request -- only the binding was missing, which is the
             same shape as DisableLogMessage in EmoteQuiet and the menu-free interact.
            
             ## ⚠ Sending a packet on somebody's behalf deserves its guards
            
             This is the identical request the client fires when you open the Achievements window, so it is not
             exotic traffic -- but it is still unprompted, so:
            
             - ⚠ **Once per login.** The flag is set before the call, not after, so a throw cannot produce a
               retry loop.
             - ⚠ **Only when it is actually needed.** If IsLoaded() is already true, nothing is sent.
             - ⚠ **Never retried.** If it does not take, the user opens the window as they always did.
             - ⚠ **Logged either way**, because a plugin that talks to the server silently is the wrong kind of
               quiet.
             </summary>
        </member>
        <member name="F:DeserokUtils.Features.AchievementData.AchievementPreload.Settle">
            ⚠ The client is not ready for this the instant Login fires, so the attempt is deferred rather
            than fired on the event. Five seconds, checked on the existing tick, no new timer.
        </member>
        <member name="T:DeserokUtils.Features.CastWatch.ActionWatcher">
             <summary>
             Hooks ActionManager.UseAction and records whether the ONE currently-armed action was accepted.
            
             Why a hook and not a poll: an instant cast (Swiftcast Raise, any oGCD like Aurora) never
             produces an observable cast bar, so IsCasting is false for exactly the case this exists to
             catch. The hook is the only thing that sees it.
            
             Why ONE armed action and no list: the game runs a single macro at a time -- starting another
             cancels the first -- so a second slot could never be reached. See README.
             </summary>
        </member>
        <member name="T:DeserokUtils.Features.CastWatch.ActionWatcher.UseActionDelegate">
            <summary>
            Signature taken verbatim from FFXIVClientStructs.xml shipped in the local Dalamud dev
            folder, NOT from memory:
              ActionManager.UseAction(ActionType, UInt32, UInt64, UInt32, UseActionMode, UInt32, Boolean*)
            If a game patch changes it, this fails to compile rather than silently mis-reading arguments.
            </summary>
        </member>
        <member name="F:DeserokUtils.Features.CastWatch.ActionWatcher.Expiry">
            <summary>How long an arm survives before it expires on its own.</summary>
        </member>
        <member name="P:DeserokUtils.Features.CastWatch.ActionWatcher.WatchedType">
             <summary>
             Action or Item, and it is NOT optional to track.
            
             ⚠⚠ The id spaces are separate: action 4571 and item 4571 are unrelated things. Matching on
             id alone would have made a watch on any action silently fire on some arbitrary item sharing
             its number -- rare, undebuggable, and wrong in the direction that sends a callout you did
             not earn.
             </summary>
        </member>
        <member name="P:DeserokUtils.Features.CastWatch.ActionWatcher.SawAttempt">
            <summary>True once the watched action was attempted at all, whatever the outcome.</summary>
        </member>
        <member name="P:DeserokUtils.Features.CastWatch.ActionWatcher.LastResult">
            <summary>What UseAction returned for the LAST attempt. See Attempts before trusting it.</summary>
        </member>
        <member name="P:DeserokUtils.Features.CastWatch.ActionWatcher.Attempts">
             <summary>
             How many times the watched action was attempted since arming.
            
             ⚠ Not decoration. A "ghetto queue" macro repeats the same /ac a dozen times to beat
             animation lock, so the FIRST attempt succeeds and the rest fail against the cooldown.
             Reporting only the last result would read fired=True returned=False and look like a bug in
             the plugin rather than the shape of the macro.
             </summary>
        </member>
        <member name="P:DeserokUtils.Features.CastWatch.ActionWatcher.FiredTargetId">
            <summary>
            The target of the attempt that actually succeeded -- NOT the last one attempted, and not
            the current target at check time. Zero if nothing succeeded.
            ⚠ This is the target the CLIENT REQUESTED, not confirmation of where the action resolved.
            </summary>
        </member>
        <member name="P:DeserokUtils.Features.CastWatch.ActionWatcher.Context">
            <summary>Who the placeholders pointed at when the watch was armed. See WatchContext.</summary>
        </member>
        <member name="P:DeserokUtils.Features.CastWatch.ActionWatcher.Filter">
            <summary>Which targets count as a hit. Any, unless /watch was given a filter flag.</summary>
        </member>
        <member name="P:DeserokUtils.Features.CastWatch.ActionWatcher.FilteredOut">
            <summary>
            Successful uses rejected by the filter. Counted apart from Attempts so that "it went off, to
            the wrong person" never reads as "it never went off".
            </summary>
        </member>
        <member name="P:DeserokUtils.Features.CastWatch.ActionWatcher.ArmIsLive">
            <summary>True when the arm is live and has not aged out.</summary>
        </member>
        <member name="M:DeserokUtils.Features.CastWatch.ActionWatcher.MatchesWatch(FFXIVClientStructs.FFXIV.Client.Game.ActionType,System.UInt32,System.UInt32@,System.UInt32@)">
             <summary>
             Whether an observed action is the one being watched.
            
             ⭐ ONE copy of this rule, because the second copy is what broke it. The upgrade-chain
             match lived only here in the detour, while the hardcast check in CastWatchFeature compared
             raw id against raw id -- so the level-sync fix worked for instants and oGCDs and silently
             did not for anything with a cast bar, which is the case a callout is most wanted for.
            
             ⚠ <paramref name="adjusted"/> and <paramref name="watchedAdjusted"/> come back out because
             the diagnostic prints them. They are not a second answer -- the bool is the answer.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.CastWatch.ActionWatcher.NormalizeItemId(System.UInt32)">
            <summary>HQ items are the same item at +1,000,000. Same thing for our purposes.</summary>
        </member>
        <member name="T:DeserokUtils.Features.CastWatch.CastWatchFeature">
             <summary>
             Gate a macro line on whether an action actually went off.
            
               /watch "Nascent Flash"
               /ac "Nascent Flash" &lt;t&gt;
               /wait 1
               /ifwatch /p Nascent Flash on &lt;t&gt;
            
             It never composes anything: the line is yours, and the only decision is whether it runs.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.CastWatch.CastWatchFeature.Section(System.String)">
            <summary>ImGui.SeparatorText does not exist in this binding version; this is the stand-in.</summary>
        </member>
        <member name="M:DeserokUtils.Features.CastWatch.CastWatchFeature.ParseFilter(System.String)">
             <summary>
             Pull a trailing filter flag off the action name: /watch Aurora --notself
            
             ⚠ An UNRECOGNISED --flag is an error, not something to ignore. Silently treating
             "--notslef" as part of the action name would fail the name lookup and report that no such
             action exists, which points at the wrong half of the line entirely.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.CastWatch.CastWatchFeature.SubstituteWho(System.String,DeserokUtils.Features.CastWatch.WatchContext,System.UInt64)">
             <summary>
             Fill {who} with whoever actually received the action.
            
             ⭐ This is the one thing a vanilla macro cannot do. &lt;mo&gt; on the callout line evaluates
             when THAT line runs, so a fallback macro that fell through from &lt;mo&gt; to &lt;2&gt; would
             announce your mouseover while someone else got the heal -- and that is exactly the case
             worth being right about. The snapshot knows the answer; this puts it in your sentence.
            
             ⚠ Braces, not angle brackets: &lt;who&gt; would collide with the game's own placeholder
             syntax, and a token the chat pipeline tries to parse is a token that breaks in ways nobody
             can read.
            
             ⚠ An unresolvable name degrades to "someone" rather than suppressing the line. A callout
             that is slightly vague still does its job; a callout that silently never went out does not,
             and a name lookup is far too small a reason to eat one.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.CastWatch.CastWatchFeature.Resolve(System.String)">
            <summary>
            Name -> action id, matching how the name reads on the hotbar. Restricted to player actions
            so a watch cannot silently bind to some internal ability that shares a name.
            </summary>
        </member>
        <member name="M:DeserokUtils.Features.CastWatch.CastWatchFeature.ResolveItemId(System.String)">
            <summary>
            Items that can be USED, which is the only kind worth watching. Phoenix Down is the reason
            this exists: it is a long cast, it rezzes, and the person using one is usually a DPS nobody
            expects to be rezzing at all -- which is exactly when a callout earns its keep.
            ⚠ Restricted to rows with an ItemAction, or every piece of gear and every crafting material
            becomes a candidate name.
            </summary>
        </member>
        <member name="M:DeserokUtils.Features.CastWatch.CastWatchFeature.IsCastingWatched(DeserokUtils.Features.CastWatch.TargetFilter,DeserokUtils.Features.CastWatch.WatchContext)">
             <summary>
             The hardcast case: nothing has resolved yet, but the cast bar is up and it is the watched
             action. Instant casts never reach this -- the hook catches those instead.
            
             ⚠ Matching goes through <see cref="M:DeserokUtils.Features.CastWatch.ActionWatcher.MatchesWatch(FFXIVClientStructs.FFXIV.Client.Game.ActionType,System.UInt32,System.UInt32@,System.UInt32@)"/> rather than comparing ids
             here. It used to compare raw id against raw id, which meant the level-sync fix covered
             instants and oGCDs and quietly missed every hardcast -- the exact failure it was written
             for, on the half of the feature where a callout matters most.
            
             ⚠ The cast bar also reports its own CastActionType, which is NOT read. The observed type is
             taken as the watched type, so the type comparison is a no-op on this path and behaviour is
             whatever it has always been. Whether that byte is the same enumeration is unmeasured, so it
             is printed by the diagnostic instead of trusted -- same treatment the adjust direction got.
             </summary>
        </member>
        <member name="T:DeserokUtils.Features.CastWatch.TargetFilter">
             <summary>
             Which targets count as a hit.
            
             ⚠ Only EXACT tests here, deliberately. "Friendly" and "hostile" inferred from ObjectKind put
             pets, chocobos, friendly NPCs and event objects in a grey zone; identity with yourself and
             membership in the party roster are id comparisons that cannot be wrong.
             </summary>
        </member>
        <member name="F:DeserokUtils.Features.CastWatch.TargetFilter.Any">
            <summary>Any target at all. The default.</summary>
        </member>
        <member name="F:DeserokUtils.Features.CastWatch.TargetFilter.Self">
            <summary>Only counts when it went to you.</summary>
        </member>
        <member name="F:DeserokUtils.Features.CastWatch.TargetFilter.NotSelf">
            <summary>Only counts when it went to anyone BUT you -- this is what catches a self-redirect.</summary>
        </member>
        <member name="F:DeserokUtils.Features.CastWatch.TargetFilter.Party">
            <summary>Only counts when it went to a party member other than you.</summary>
        </member>
        <member name="T:DeserokUtils.Features.CastWatch.WatchContext">
             <summary>
             Who the targeting placeholders pointed at, captured the moment /watch ran.
            
             ⚠ Why a snapshot and not a live read: a fallback macro resolves in well under a second, but by
             the time it reaches a later line the mouse has moved. Reading &lt;mo&gt; at check time answers
             "where is the cursor now", which is a different question from "who did the spell go to".
            
             deserok's scoping, and it is the right one: a long cast-sequence macro is out of scope for this,
             and should wrap the watch tightly around the one action it cares about instead.
             </summary>
        </member>
        <member name="P:DeserokUtils.Features.CastWatch.WatchContext.Party">
            <summary>Party slots as the game numbers them: index 1 is &lt;1&gt; (you), 2 is &lt;2&gt;.</summary>
        </member>
        <member name="M:DeserokUtils.Features.CastWatch.WatchContext.Describe(System.UInt64)">
            <summary>
            Which placeholder the given id corresponds to, as the macro would have written it. This is
            the thing a fallback macro cannot tell you on its own: whether it hit your mouseover or fell
            through to a party slot.
            </summary>
        </member>
        <member name="M:DeserokUtils.Features.CastWatch.WatchContext.Passes(DeserokUtils.Features.CastWatch.TargetFilter,System.UInt64,System.UInt64)">
             <summary>
             Does this target satisfy the filter?
            
             ⚠⚠ Evaluated per ATTEMPT, not once at the end. deserok's fallback macro tries &lt;mo&gt;
             seven times then &lt;2&gt; six times; if the mouseover is invalid the game may redirect the
             early attempts to you, and the later one reaches the healer. Judging only the first success
             would call that a self-cast and suppress a callout that should have gone out.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.CastWatch.WatchContext.NameOf(System.UInt64)">
            <summary>
            Just the name, for putting into a chat line. Describe() is for diagnostics and says too much
            ("&lt;mo&gt; Peachi Bunni = &lt;2&gt; Peachi Bunni"); this is what a person would type.
            </summary>
        </member>
        <member name="T:DeserokUtils.Features.DebuffMarks.DebuffMarksFeature">
             <summary>
             Put an icon over the head of anyone carrying a status you care about.
            
             ## Why it exists
            
             Bunny plays Samurai in PvP, where the limit break <b>Zantetsuken</b> reads: *"If target is
             afflicted with Kuzushi, deals damage equal to 100% of their maximum HP."* So the question she needs
             answered mid-fight is "which of these people is marked for execution" -- and PvP moves faster than
             reading debuff icons off a target frame.
            
             deserok, setting the boundary before anyone asked: *"I can maybe make something that puts an icon
             over their heads for you, but I will never make something that presses the LB for you."* ⭐ That is
             the same read-only line that made WeakAuras safe to share with strangers, and it is worth holding
             deliberately rather than by accident.
            
             ## ⚠⚠ SourceId is not optional, because Kuzushi is source-attributed
            
             The status text says *"Damage taken from **the samurai who applied this effect** is increased."*
             So "does this target have status 3202" is the WRONG question -- another Samurai's Kuzushi will not
             make your limit break execute. Marking on presence alone would be confidently wrong at the exact
             moment somebody spends an LB on it, which is worse than showing nothing.
            
             ## ⚠ Scanning, throttled rather than per-frame
            
             A Frontline holds ~72 players and each carries up to thirty statuses. Checking all of that every
             frame is the per-frame audit this project keeps re-learning, somewhere it would actually hurt. The
             scan runs at 10 Hz and the draw path reads the result -- the same shape as EphemeralMarks' 1 Hz
             resolve, but faster, because a four-second debuff cannot wait a second.
            
             ⭐ Reuses <see cref="T:DeserokUtils.Features.EphemeralMarks.MarkShapes"/> and <see cref="T:DeserokUtils.Features.EphemeralMarks.MarkFont"/> unchanged: the glyph rendering, the
             halo, and the rasterise-at-drawn-size fix are Frontline-tested already.
            
             ⚠ The position and smoothing loop IS duplicated from EphemeralMarks, deliberately and with a
             trigger: extract a shared overlay when a THIRD feature wants one. Refactoring a feature two people
             are actively testing, to save forty lines, is the wrong trade today.
             </summary>
        </member>
        <member name="F:DeserokUtils.Features.DebuffMarks.DebuffMarksFeature.resolved">
             <summary>
             Name -&gt; every status id that carries it, resolved once per name.
            
             ⚠⚠ A SET, not one id. Three separate statuses are called "Reprisal" and two are called
             "Kuzushi"-adjacent; picking one silently watches the wrong number and the feature looks broken
             rather than misconfigured. ⭐ Watching all of them is correct anyway: they are the same effect
             with different provenance, and the source filter is what narrows it to yours.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.DebuffMarks.DebuffMarksFeature.Tick">
            <summary>
            ⚠ Leaves immediately when nothing is watched. A feature switched off or left unconfigured must
            cost one bool test, not a walk of the object table.
            </summary>
        </member>
        <member name="M:DeserokUtils.Features.DebuffMarks.DebuffMarksFeature.Smooth(System.UInt64,System.Numerics.Vector2,System.Single)">
            <summary>
            ⚠ The adaptive filter from EphemeralMarks, needed for the same reason: both position sources
            oscillate sub-pixel against the game's own nameplate. A FIXED smoother cannot win -- heavy
            enough to kill the wobble is heavy enough to lag every camera pan -- so it reads the frame delta
            and picks its own strength.
            </summary>
        </member>
        <member name="M:DeserokUtils.Features.DebuffMarks.DebuffMarksFeature.IdsFor(System.String)">
             <summary>
             Every status id carrying this name, matched case-insensitively and memoised.
            
             ⚠ Empty when nothing matches, which is what the tab reports as "no such status".
            
             ⚠ The sheet walk happens once per distinct name, not per scan. It is a few thousand string
             extractions and belongs nowhere near a 10 Hz loop.
             </summary>
        </member>
        <member name="T:DeserokUtils.Features.DrawSheathe.DrawSheatheFeature">
             <summary>
             One key that draws or sheathes, whichever is currently correct -- and picks the fancy emote or
             the game's own toggle depending on whether the emote would actually play.
            
               /drawsheathe
            
             ⚠⚠ THE GOLD SAUCER DRAW AND SHEATHE ARE EMOTES, NOT THE WEAPON TOGGLE. Confirmed from the game's
             own Emote sheet 2026-08-17: row 237 "Sheathe Weapon" -> /sheathe, row 238 "Draw Weapon" ->
             /draw, and those two rows are the ONLY draw/sheathe entries in the sheet. Buying them does not
             replace the default animations, it adds two independent emotes -- so the default draw/sheathe
             KEYBIND cannot play them, and an emote cannot be bound as a toggle. Two hotbar slots, and you
             have to know which one is currently correct. That is the problem this removes.
            
             ⭐ This already worked via TinyCommands: `/ifcmd -r /sheathe motion` + `/ifcmd -R /draw motion`.
             The missing thing was never functionality, it was OWNERSHIP -- that maintainer has quit several
             times and takes months to ship even a version bump, so every patch leaves a working setup broken
             at someone else's pace. Here, patch day is a recompile deserok controls.
            
             ⚠⚠ AND THE EMOTES DO NOTHING WHILE MOVING. That is what the macro could not solve and is why
             this is not simply a reimplementation of it: the emote is silently refused, so the key looks
             broken exactly when you are running somewhere. Hence the second branch -- moving means fall back
             to the game's own instant toggle, which always works. One key, and the choice between "fancy"
             and "fast" stops being something to make in your head.
            
             ⚠ Scope: one keypress, one action. No loop, no queue, no reaction to game events. The state
             reads and the branch are the whole feature.
             </summary>
        </member>
        <member name="F:DeserokUtils.Features.DrawSheathe.DrawSheatheFeature.lastPressAt">
            <summary>When the last press EVENT arrived, accepted or dropped. See PressTooSoonBecause.</summary>
        </member>
        <member name="M:DeserokUtils.Features.DrawSheathe.DrawSheatheFeature.WeaponIsOut">
             <summary>
             Whether the weapon is out, or null if there is nobody to ask.
            
             ⭐ `StatusFlags.WeaponOut` off the managed Dalamud object, NOT a pointer walk. It is also the
             exact flag TinyCommands' `-r` reads, which is what decided it: this is the signal already
             proven correct for this job on this character, not a second candidate that ought to agree.
            
             ⚠ `UIState.WeaponState.IsUnsheathed` is that second candidate and it is deliberately NOT used
             for the decision, even though the toggle branch calls a method on that very struct. Mixing
             signals would make the answer depend on which branch ran, which is the harder bug of the two.
             The tab shows both side by side so a disagreement is visible instead of silent.
            
             ⚠ Null is NOT false. "Not logged in" and "weapon sheathed" would both draw, and one of those
             is a bug that reports itself as the feature working. The caller refuses instead.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.DrawSheathe.DrawSheatheFeature.ClientSaysUnsheathed">
            <summary>
            The client's own copy of the same fact, for cross-checking in the tab. See
            <see cref="M:DeserokUtils.Features.DrawSheathe.DrawSheatheFeature.WeaponIsOut"/> for why this is not what the branch reads.
            </summary>
        </member>
        <member name="M:DeserokUtils.Features.DrawSheathe.DrawSheatheFeature.PlayerIsMoving">
             <summary>
             Whether the player is moving, or null if it cannot be read.
            
             ⚠ `AgentMap.IsPlayerMoving`, and the agent is an odd-looking place for it -- but it is the
             field the client keeps for exactly this and it is populated whether or not the map is open.
             There is no movement flag on ICondition and no `IsMoving` anywhere in the Dalamud surface;
             this was found by reflecting over FFXIVClientStructs.dll rather than assumed.
            
             ⚠ What counts as "moving" here is the client's opinion, not ours, and it has not been
             characterised against jumping, mounts or knockbacks. The diagnostic line prints it on every
             press so a surprising case shows itself rather than being argued about.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.DrawSheathe.DrawSheatheFeature.SheatheCooldown">
             <summary>
             The game's own sheathe cooldown. Displayed in the tab, and gated on by PressTooSoonBecause.
            
             ⭐ SETTLED 2026-08-17, BY WATCHING IT RATHER THAN GUESSING: it counts DOWN. Traced at 0 while
             idle, set to 1.0 by a weapon-state change, and decaying at 1.0/second -- `0.895 -> 1`,
             `0.487 -> 1`, `0.583 -> 1` across a burst, and 0.891 exactly 102 ms after a change. Both paths
             set it: the queued emote does so as surely as the direct call. So `> 0` is safe to gate on and
             PressTooSoonBecause now does.
            
             ⚠ It shipped as "displayed, not obeyed" for exactly one build, because FFXIVClientStructs
             documents neither the unit nor the sign convention of this float, and "seconds elapsed since
             the last change" -- which only ever grows -- was as plausible a reading as "seconds
             remaining". Gating on `> 0` under THAT reading would have dead-locked the key on the first
             press and looked identical to the spam bug being fixed. One round of evidence cost nothing
             and removed the ambiguity completely.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.DrawSheathe.DrawSheatheFeature.PressTooSoonBecause">
             <summary>
             Why this press must be dropped, or null to let it through -- the spam guard.
            
             ⚠⚠ HOLDING THE KEY IS A REAL INPUT MODE HERE. deserok uses a key repeater as an assistive
             device -- tapping is difficult -- and it behaves like ordinary auto-repeat: a tap is one
             press, but holding past about half a second streams presses at roughly 10 Hz (102 ms apart,
             measured) until release. So a normal press is NOT a burst, and the collapse below never
             touches one; it exists for the hold, where a dozen toggles a second is what was observed.
            
             ⚠⚠ A cooldown ALONE does not fix the hold, and shipping only that would have looked fixed. A
             one-second gate against a stream does not stop the cycling, it slows it to one toggle per
             second and keeps going for as long as the key is down. The stream has to COLLAPSE.
            
             ⭐ So the first rule is a quiet-gap test, about the input device rather than the game: act on
             the first press, then ignore everything until the key has been quiet for
             <see cref="P:DeserokUtils.Configuration.DrawSheatheRepeatCollapseMs"/>. A held key never reaches that gap and
             so acts exactly once; releasing and pressing again does. Configurable because repeat rates are
             a property of somebody's hardware, and this is an accessibility setting rather than a tuning
             detail to bury in a constant.
            
             ⭐⭐ THE SECOND RULE IS THE GAME'S OWN COOLDOWN, MEASURED -- see <see cref="M:DeserokUtils.Features.DrawSheathe.DrawSheatheFeature.SheatheCooldown"/>
             for the trace that settled which direction it counts. It counts DOWN, and BOTH paths set it:
             the emote does it as surely as the direct call. So one gate covers both, with no invented
             constant, and it is the same one second the default keybind enforces -- which is why the
             default key never glitched.
            
             ⚠⚠ It replaced a guard that waited for the weapon state to reach what the last press asked
             for. That guard was not wrong, it was just too fast to help: the flag flips within ~100 ms, so
             it cleared before the next repeat arrived and the cycle continued underneath it. Deleted
             rather than left beside this one -- two rules that can disagree about the same press is worse
             than either alone.
            
             ⭐ A refused action sets no cooldown, so it does not lock you out. The old guard armed itself
             on a request it could not confirm and burned two seconds when the request went nowhere.
            
             ⚠ Unreadable means allow. Falling back to "block" would make an unavailable UIState look
             exactly like the dead key this exists to prevent.
            
             ⚠ It WRITES as well as reads -- every press event stamps the clock, dropped ones included,
             because a repeat that was ignored is still evidence the key is down. Impure for a "Because"
             method, and called from exactly one place for that reason.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.DrawSheathe.DrawSheatheFeature.CommandFor(System.Boolean)">
            <summary>
            Which emote command a press would send right now. ⭐ One place, so the ownership check and the
            send cannot disagree about which emote is in question -- checking one and sending the other is
            exactly the bug this was added to fix.
            </summary>
        </member>
        <member name="M:DeserokUtils.Features.DrawSheathe.DrawSheatheFeature.EmoteRefusedBecause(System.String)">
             <summary>
             Why the emote would be refused right now, or null if it would play.
            
             ⚠⚠ THE QUESTION IS NOT "AM I MOVING". It only looked that way because moving was the first
             case found. Jumping on the spot refuses the emote too and reads `IsPlayerMoving == false` --
             so a check named after movement was answering a narrower question than the one being asked,
             and would have kept being wrong one new case at a time.
            
             ⭐ Returning a REASON rather than a bool is the point. Every branch says out loud which
             condition sent it down the fast path, so the next case that turns up (falling? gliding?
             mounted?) is named in the diagnostic line instead of being inferred from the key misbehaving.
             `/drawsheathe conditions` lists everything the game currently has set, for exactly that.
            
             ⚠ This is a list of OBSERVED refusals, not a model of the game's rule. It cannot be complete
             and is not trying to be: an unlisted case simply gets the emote, which fails harmlessly and
             is the behaviour that existed before any of this.
            
             ⚠⚠⚠ DESPITE THE NAME, THIS IS NOT "EVERY CASE WHERE THE EMOTE FAILS". It is "cases where
             falling back to the direct call is CORRECT", and those two sets are not the same. **Cutscenes
             are the counterexample and must stay off this list.** Tested 2026-08-17: typing the command
             during one gets a clean refusal from the game -- *The command "/draw motion" is unavailable at
             this time.* -- which is the right outcome, delivered with a reason. Adding cutscenes here
             would route them to SetUnsheathed, which is a raw state write that consults none of that, and
             would force the weapon out mid-scene. The "consistency fix" is the bug.
            
             ⭐ So the test for adding a case is not "does the emote fail here" but "should the game's own
             toggle happen here instead". Moving and jumping pass it: you asked to draw, and drawing while
             running is ordinary. A cutscene fails it: the game is saying no on purpose.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.DrawSheathe.DrawSheatheFeature.DumpConditions">
             <summary>
             Every condition flag the game currently has set.
            
             ⭐ Here because the refusal list above cannot be complete, and the alternative to this is
             guessing which flag names the case you just hit. Same argument as the sniffer: when a new
             refusal turns up, read what the game says rather than reasoning about what it might be.
            
             ⚠ Prints only what is SET. The full enum is ~90 entries, most of them off, and a wall of
             False is how a useful diagnostic becomes one nobody runs.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.DrawSheathe.DrawSheatheFeature.PlayEmote(System.Boolean,System.String)">
            <summary>The Gold Saucer animation. Silently refused in the cases EmoteRefusedBecause lists, which is why the branch exists.</summary>
        </member>
        <member name="M:DeserokUtils.Features.DrawSheathe.DrawSheatheFeature.FastToggle(System.Boolean,System.String)">
             <summary>
             The game's own draw/sheathe -- what the default keybind does, from code, because the sheet has
             no text command for it. Only `/autosheathe`, `/draw` and `/sheathe` mention it, and the last
             two are the emotes.
            
             ⚠⚠⚠ THE ARGUMENTS ARE COPIED FROM A RECORDING OF THE REAL KEYBIND, AND `isInstant` MEANS THE
             OPPOSITE OF WHAT IT IS CALLED.
            
               newState   -- true = unsheathed. So it is the OPPOSITE of what we just read.
               sendPacket -- whether to send a network update. TRUE: other players must see it, and the
                             default keybind is not a client-side illusion.
               isInstant  -- TRUE. Read the name and this is obviously the flag that skips the animation,
                             so it shipped as false. That teleported the weapon into the hand with no
                             animation at all -- the exact effect the name promises for `true`.
            
             ⭐⭐ The sniffer settled it in one press. The game's own keybind passes `isInstant: true`
             EVERY time, standing still and moving alike (recorded 2026-08-17, eight presses, no
             variation), and that is the call that animates. Whatever the flag really selects, it is not
             "skip the animation" -- and no amount of staring at the parameter list would have said so.
            
             ⚠ So the arguments do NOT vary with movement. The refusal reason chooses BETWEEN the emote and
             this function; it changes nothing about how this function is called. A future reader looking
             for the "moving" variant of the call should stop here: there isn't one.
            
             ⚠ It returns a bool. Nothing here can act on a refusal -- there is no second thing to try --
             but a false is logged, because "the key did nothing" and "the client said no" look identical
             from the outside and only one of them is worth investigating.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.DrawSheathe.DrawSheatheFeature.OnSniff(System.String)">
            <summary>
            Arm the recorder, then go press the REAL draw/sheathe key -- the game's own keybind, not this
            command. Whatever the client calls on the way to the animation shows up in chat.
            </summary>
        </member>
        <member name="M:DeserokUtils.Features.DrawSheathe.DrawSheatheFeature.Trace(System.String)">
             <summary>
             Every press outcome, to BOTH the log and the diagnostic channel.
            
             ⚠⚠ The log half is not redundant. Diag is off by default, so the first round of "it does
             nothing when moving" produced a completely empty dalamud.log -- no evidence at all, and the
             only way forward was to ask for the toggle and repeat the test. A keypress is a rare event;
             one line each is nothing next to being able to read what happened after the fact.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.DrawSheathe.DrawSheatheFeature.Section(System.String)">
            <summary>ImGui.SeparatorText does not exist in this binding version; this is the stand-in.</summary>
        </member>
        <member name="T:DeserokUtils.Features.DrawSheathe.EmoteUnlock">
             <summary>
             Whether the character actually owns the emote a press would send.
            
             ## ⚠⚠⚠ Found by Bunny, 2026-08-22, on the first day anyone else ran this
            
             She owns **Draw Weapon** and not **Sheathe Weapon** -- they are two separate Gold Saucer rewards,
             emote 238 and emote 237. Drawing worked; sheathing did nothing at all. Not the game's toggle, not
             an error, nothing.
            
             ⭐⭐ And this is a **new shape of blind spot**, distinct from the one already recorded in
             ORIENTATION. That one was "needs a second player". This one is *needs a different account* -- the
             feature was correct for every state deserok's character can be in, because his character owns
             both halves and no amount of testing alone can reach the state where it does not. ⚠ The general
             form is worth remembering: **anything gated on unlocks, progression or purchases is untestable
             from one account**, and the failure is silent because the game simply refuses the command.
            
             ⭐ The fix is not a new mechanism. <see cref="M:DeserokUtils.Features.DrawSheathe.DrawSheatheFeature.EmoteRefusedBecause(System.String)"/> already
             exists to answer "should the game's own toggle happen here instead", and an emote you do not own
             is the cleanest possible yes: you asked to sheathe, the fancy version is unavailable, so sheathe
             normally. It is one more reason string in a predicate built to collect them.
             </summary>
        </member>
        <member name="F:DeserokUtils.Features.DrawSheathe.EmoteUnlock.byCommand">
             <summary>
             Command -&gt; emote, from the Emote sheet's own TextCommand link.
            
             ⚠ Read from the sheet rather than hardcoding 237 and 238, because the two commands are
             CONFIGURABLE -- someone can point this at any emote they like, and a hardcoded pair would
             check the wrong thing the moment they did. Same reason the emote list was dumped from the
             sheet rather than copied off a wiki.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.DrawSheathe.EmoteUnlock.LockedBecause(System.String)">
             <summary>
             Why this command cannot be played, or null if it can be -- or if we cannot tell.
            
             ⚠⚠ NULL MEANS "GO AHEAD" IN THREE DIFFERENT CASES: the emote is owned, the command is not an
             emote we recognise, or UIState could not be read. All three keep the pre-existing behaviour,
             which is to send the command and let the game answer. ⭐ Refusing on a failed read would turn
             a momentary unreadable state into a key that does nothing, which is the exact symptom being
             fixed here.
             </summary>
        </member>
        <member name="T:DeserokUtils.Features.DrawSheathe.WeaponStateSniffer">
             <summary>
             Watch what the GAME does when you press the real draw/sheathe key, instead of arguing about it.
            
             ⭐⭐ This exists because reasoning lost, and it won in one press. `SetUnsheathed(newState,
             sendPacket: true, isInstant: false)` looked like an exact description of "draw normally, with the
             animation" -- the parameter names are FFXIVClientStructs' own -- and it teleported the weapon
             into the hand. The recording showed the real keybind passing `isInstant: TRUE` every time, which
             is the call that animates. The flag does not mean what it is named, and nothing short of
             watching the game do it would have said so.
            
             ⚠ Kept rather than deleted once it had answered. It cost one file, it is inert unless armed, and
             the next question of this shape -- "what does the client actually do when I press that?" -- is
             one keypress away instead of one build away.
            
             ⚠ Only two of WeaponState's functions have a resolvable address -- SetUnsheathed and
             SetUnsheathed2. OnActorControlWeaponDrawn and Tick are declared but carry no signature, so they
             cannot be hooked. That bounds what this can prove, and the bound is informative: if pressing the
             key logs NEITHER call, the animation is not driven from WeaponState and looking there any
             further is wasted.
            
             ⚠ Armed, never permanent. Both functions are cold -- they fire when you draw or sheathe and at
             no other time -- so an idle hook here would be far cheaper than the UseAction one CastWatch had
             to fix. It is still armed on demand, because a diagnostic that installs itself at load is how
             you end up with the per-frame audit again.
             </summary>
        </member>
        <member name="T:DeserokUtils.Features.DrawSheathe.WeaponStateSniffer.SetUnsheathedDelegate">
            <summary>
            Signatures verbatim from FFXIVClientStructs, NOT from memory:
              WeaponState.SetUnsheathed(Boolean newState, Boolean sendPacket, Boolean isInstant) : Boolean
            A patch that changes them breaks the build instead of silently mis-reading arguments.
            </summary>
        </member>
        <member name="T:DeserokUtils.Features.DrawSheathe.WeaponStateSniffer.SetUnsheathed2Delegate">
            <summary>WeaponState.SetUnsheathed2(Boolean newState) : Boolean</summary>
        </member>
        <member name="F:DeserokUtils.Features.DrawSheathe.WeaponStateSniffer.DefaultDuration">
            <summary>How long an arm lasts if nothing disarms it sooner.</summary>
        </member>
        <member name="M:DeserokUtils.Features.DrawSheathe.WeaponStateSniffer.ExpireIfDue">
             <summary>
             Expire lazily rather than on a Tick.
            
             ⚠ Which means an arm nobody follows up on survives until the next draw or the next time the
             tab is open. That is deliberate: adding a per-frame callback to carry a diagnostic's egg timer
             is exactly the shape of work this project has already had to strip out twice, and these two
             functions cost nothing while idle.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.DrawSheathe.WeaponStateSniffer.Record(System.String,System.Boolean,FFXIVClientStructs.FFXIV.Client.Game.UI.WeaponState*)">
            <summary>
            ⚠ Reports the state BEFORE and AFTER around the original call, not just the arguments. "What
            was passed" and "what actually changed" are two different facts, and the whole reason this
            exists is that a call which looked right did not produce the effect its arguments described.
            </summary>
        </member>
        <member name="T:DeserokUtils.Features.EmoteQuiet.EmoteInterceptor">
             <summary>
             Sets <c>PlayEmoteOption.DisableLogMessage</c> on a repeat of an emote you have already announced,
             so the first clap talks and the next fifty do not.
            
             ## ⭐⭐ Everything below was MEASURED, 2026-08-17, before a line of it was written
            
             Recorded at this exact hook, one press per row:
            
             <code>
             /clap                 Flags=0x00  DisableLogMessage=False
             /clap motion          Flags=0x01  DisableLogMessage=True
             emote window click    Flags=0x88  DisableLogMessage=False   (AgentEmote got option=NULL)
             hotbar slot           Flags=0x00  DisableLogMessage=False   (AgentEmote got option=NULL)
             </code>
            
             ⭐ **`motion` IS this flag and nothing else.** The two text-command calls reach this function
             with identical arguments except bit 0. Since this is the funnel through which the emote actually
             executes, and the command's only influence on it is these arguments, that bit is the whole
             mechanism -- so setting it ourselves is exactly what typing `motion` does.
            
             ⚠⚠ **NEVER ASSIGN Flags WHOLESALE.** The emote window arrives with `0x88` -- bits 3 and 7 set,
             meaning something this code knows nothing about. Writing `Flags = 0x01` would silently clear them.
             Only the `DisableLogMessage` property is ever touched, because it sets one bit and leaves the
             rest alone. This was caught solely because the recording printed the raw byte next to the
             friendly property; the friendly property alone said `False` in both cases and looked identical.
            
             ⚠⚠ **Hooked at EmoteManager, not AgentEmote, and that was measured too.** `AgentEmote.ExecuteEmote`
             receives `option = NULL` for the emote window and hotbar and builds the struct on the way down --
             so a hook there would have nothing to modify on exactly the paths that matter most. A second hook
             on AgentEmote existed while this was being investigated and was DELETED once it had answered:
             nothing acts on that layer, and a detour that only observes is a detour to remove.
            
             ⚠ Still unconfirmed by anything inside this client: whether other players stop seeing the line.
             Nothing here can read someone else's chat log. The argument above is tight, but the acceptance
             test is a person standing next to you.
             </summary>
        </member>
        <member name="T:DeserokUtils.Features.EmoteQuiet.EmoteInterceptor.ExecuteEmoteDelegate">
            <summary>
            Signature verbatim from FFXIVClientStructs, NOT from memory:
              EmoteManager.ExecuteEmote(UInt16 emoteId, EmoteController.PlayEmoteOption* playEmoteOption) : Boolean
            </summary>
        </member>
        <member name="F:DeserokUtils.Features.EmoteQuiet.EmoteInterceptor.lastAnnounced">
             <summary>
             When each emote FAMILY last actually announced itself, so clapping does not silence your next
             /dote -- "the same emote" is the unit, per the original ask.
            
             ⚠ Keyed by family rather than by emote id, because the Cheer variants are sixteen ids that
             nobody treats as sixteen emotes. See <see cref="P:DeserokUtils.Configuration.EmoteQuietFamilies"/>.
             </summary>
        </member>
        <member name="F:DeserokUtils.Features.EmoteQuiet.EmoteInterceptor.familyCache">
            <summary>emote id -> (family key, label). Memoised: the sheet lookup is the same answer every
            time and this runs inside a detour.</summary>
        </member>
        <member name="M:DeserokUtils.Features.EmoteQuiet.EmoteInterceptor.Sync">
            <summary>
            ⭐ ONE hook serving both jobs. The suppressor and the recorder want the same detour on the
            same cold function, so installing two would mean two detours where one does. Enabled while
            either wants it, disabled when neither does -- see the per-frame audit in DeserokUtils.md for
            why a permanently-installed hook is not the default here.
            </summary>
        </member>
        <member name="M:DeserokUtils.Features.EmoteQuiet.EmoteInterceptor.Reset">
            <summary>Forget every timer, so the next use of anything announces again.</summary>
        </member>
        <member name="M:DeserokUtils.Features.EmoteQuiet.EmoteInterceptor.ActiveWindows">
            <summary>Emote families currently inside their quiet window, for the tab.</summary>
        </member>
        <member name="M:DeserokUtils.Features.EmoteQuiet.EmoteInterceptor.FamilyOf(System.UInt16)">
             <summary>
             Which family an emote belongs to, and what to call it.
            
             ⚠ The configured prefixes are checked against the emote NAME, longest first, so a more
             specific prefix wins over a broader one if somebody adds both. Anything unmatched is its own
             family keyed by id -- the default for every emote in the game except the Cheers.
            
             ⚠ The cache is cleared when the family list is edited (see <see cref="M:DeserokUtils.Features.EmoteQuiet.EmoteInterceptor.ForgetFamilies"/>),
             because otherwise an emote fired before the edit would keep its old family for the session
             and the setting would look like it had not applied.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.EmoteQuiet.EmoteInterceptor.ForgetFamilies">
            <summary>Drop the memoised families, so an edit to the list takes effect immediately.</summary>
        </member>
        <member name="M:DeserokUtils.Features.EmoteQuiet.EmoteInterceptor.Decide(System.UInt16,FFXIVClientStructs.FFXIV.Client.Game.Control.EmoteController.PlayEmoteOption*)">
             <summary>
             The whole rule. Returns what it did, for the log.
            
             ⚠⚠ AN EMOTE THAT WAS ALREADY SILENT DOES NOT START THE TIMER. If you typed `/clap motion`
             yourself, no message went out, so treating it as "you have announced this" would eat the next
             real one. The timer tracks announcements, not uses -- which is why the dictionary is called
             lastAnnounced and not lastUsed.
            
             ⚠ A null option is passed through untouched rather than guessed at. It has never been observed
             at this layer -- AgentEmote is where NULL shows up, and it fills the struct in before calling
             this -- but "never observed" is not "cannot happen", and the safe direction is the message
             going out when it should not rather than a crash.
            
             ⚠⚠ THE GAME'S OWN "Display log message" CHECKBOX IS NOT VISIBLE HERE. Measured 2026-08-17:
             with it OFF, `/clap` still arrives as `Flags=0x00, DisableLogMessage=False`. So that setting
             is applied somewhere downstream and is a DIFFERENT mechanism from `motion` -- which is worth
             knowing, because it means this code cannot tell whether a message actually went out.
            
             ⭐ It does suppress for EVERYONE, not just locally -- deserok has relied on that for years,
             spamming backflips with the box off and nobody ever seeing the line. Which settles the shape
             of the whole feature: the game has two independent broadcast-suppression routes, the
             all-or-nothing checkbox and the per-emote flag, and this uses the second to get what the first
             cannot express -- "say it once, then be quiet".
            
             ⚠ Hence one known edge, accepted rather than fixed: with the checkbox off, a timer is started
             for an announcement that may never have happened, so ticking the box again inside the window
             can eat one message. It self-heals in a minute and `/emotequiet reset` clears it.
             `UiConfigOption.EmoteTextType` exists and could be consulted, but nothing documents which
             value means "on" -- and inventing a direction for an undocumented flag is precisely the
             `isInstant` mistake, which cost a build the same evening. One clap in a corner case is not
             worth paying that twice.
             </summary>
        </member>
        <member name="T:DeserokUtils.Features.EmoteQuiet.EmoteQuietFeature">
             <summary>
             Announce an emote the first time, then stay quiet about repeats of that same emote for a while.
            
             ## The problem
            
             The emote log message is spam in a crowd -- a bard performance, a gathering -- so the game's
             "Display log message" checkbox gets switched off, and then nobody ever knows you clapped at all.
             The only way to have both today is tick the box, clap once, untick it. What is actually wanted,
             in deserok's words, is *"display the first of a given emote, then hide log messages for the same
             emote within 1 minute"*.
            
             ⭐ The game already has the exact per-emote switch for this and it is what `motion` sets. See
             <see cref="T:DeserokUtils.Features.EmoteQuiet.EmoteInterceptor"/> for the recording that proved it, and for the two things that
             recording caught which reasoning would not have.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.EmoteQuiet.EmoteQuietFeature.Section(System.String)">
            <summary>ImGui.SeparatorText does not exist in this binding version; this is the stand-in.</summary>
        </member>
        <member name="T:DeserokUtils.Features.EmoteQuiet.IncomingEmoteFilter">
             <summary>
             Hide OTHER people's repeated emote lines in your own log, on the same one-minute-per-emote rule
             as the outgoing half.
            
             ⭐⭐ KEYED PER SENDER, AND THAT IS THE WHOLE DESIGN, not a default to be revisited. deserok
             performs: *"knowing 10 people clapped is nice, knowing 1 guy spammed clap 30 times is noise"*.
             Audience size is the signal; one person's repetition is the noise. Keying on sender-plus-line
             keeps every distinct person and collapses only repetition, which separates those two exactly.
            
             ⚠ A sender-stripped variant -- one "claps" per minute across the whole zone -- was considered and
             deliberately NOT built. It would be quieter at a fifty-person show and would destroy the one
             thing he is actually reading the log for. A setting for it would just be a switch labelled
             "delete the useful half".
            
             ## ⭐ Why this beats the xN collapsing in Chat 2.0
            
             That collapses *consecutive identical lines* -- its state is "the last line, and a count". So
             `A B A B A B` never repeats consecutively and nothing collapses; you get all six. This keeps an
             independent clock per distinct line, so alternation is the case it handles best rather than the
             case that defeats it. That failure is exactly what deserok reported.
            
             ⚠⚠ SUPPRESSING INCOMING CHAT IS RISKIER THAN SUPPRESSING YOUR OWN EMOTE, and in a way that hides
             its own mistakes: an outgoing bug means a message you wanted does not go out, which you notice,
             while a filter that is too broad removes things from your log invisibly. So it touches ONLY the
             two emote log kinds, never anything else, and every suppression is countable in the tab and
             loggable via diagnostics. Auditable rather than trusted.
             </summary>
        </member>
        <member name="F:DeserokUtils.Features.EmoteQuiet.IncomingEmoteFilter.lastShown">
            <summary>
            When each distinct sender-and-line last got through. See the class remarks for why the sender
            is part of the key.
            </summary>
        </member>
        <member name="P:DeserokUtils.Features.EmoteQuiet.IncomingEmoteFilter.SuppressedCount">
            <summary>How many lines have been hidden since the counter was last reset. Tab only.</summary>
        </member>
        <member name="F:DeserokUtils.Features.EmoteQuiet.IncomingEmoteFilter.SweepThreshold">
            <summary>
            ⚠ A dictionary fed by every emote line in a crowded zone grows without limit -- the same shape
            as the config lists that silently doubled on every load, arriving from a new direction. Expired
            entries are swept once the map gets big rather than on a timer, which costs nothing in the
            normal case and cannot run away in the bad one.
            </summary>
        </member>
        <member name="M:DeserokUtils.Features.EmoteQuiet.IncomingEmoteFilter.ActiveWindows">
            <summary>Lines currently inside their quiet window, for the tab.</summary>
        </member>
        <member name="T:DeserokUtils.Features.EphemeralMarks.EphemeralMarksFeature">
             <summary>
             A marker over the heads of the people you queued in with. Yours only -- nobody else sees it.
            
             Requested by Bunny 2026-08-21: the default party marks are shared and contested, *"it shows for
             everyone and everyone tends to fight for use of them"*, and in Frontlines she and Q lose deserok
             in the chaos.
            
             ## ⚠⚠ Why the game does not already solve this
            
             Nameplate colour separates your PARTY from the ALLIANCE -- it does not separate your friends from
             the five randoms assigned to your own party. deserok, correcting an earlier claim of mine:
             *"in frontlines, your premade is in your party too, but you're generally lost in the other 6 you
             get assigned with, so the blue name becomes indistinguishable."* Frontlines and alliance raids
             fail identically.
            
             ⭐ So the real condition is **how many identical-looking allies there are, and whether your
             friends are a small subset of them**:
             <code>
              4  dungeon        trivial: with one friend you are half the group
              8  normal raid    easy: eight names
             24  alliance raid  hard
             24+ Frontlines     hard, and moving
             ??  field ops      dozens, unbounded
             </code>
            
             ⭐⭐ Alliance raid is included, and **Cover is the argument that carries it**: the ability can kill
             the caster, so deserok is selective, and which ally is under the cursor changes whether the risk
             is worth taking. That is decision-relevant, not convenience.
            
             ## ⚠⚠ Deep dungeons are in for a SECOND reason, and the rule above does not cover them
            
             A deep dungeon party is four people who are all your friends, so by the table above it should be
             excluded outright -- there is nobody to be told apart from. deserok, 2026-08-22, after a
             Heaven-on-High run: *"we lose each other constantly."*
            
             ⭐ So the overlay does two jobs and only one of them was designed: **identification** in a crowd of
             lookalikes, and **wayfinding** in a dark randomised maze. The second needs no crowd at all. Worth
             stating outright, because the exclusion rules below are written for the first job and will keep
             reading as though they are the whole story.
            
             ## What it deliberately is not
            
             ⚠ Not a command to mark someone -- that means marking during the chaos, which is exactly when you
             cannot. ⚠ Not "all friends" -- floods a 72-player Frontline with people who do not matter right
             now. ⚠ Not a whitelist of people you care about -- bigger, and it means keeping a list of specific
             people, which is the direction to stay away from. "Who I came with" is self-scoping and forgets
             itself.
            
             ⚠⚠ THE PER-PERSON OVERRIDES ARE NOT AN EXCEPTION TO THAT, and the distinction is the whole reason
             they were acceptable. They are consulted only for people the snapshot ALREADY produced, and can
             change nothing except which glyph is drawn and what colour it is. The moment one influences who
             gets marked, it has become the whitelist above. See <see cref="P:DeserokUtils.Configuration.MarksOverrides"/>.
             </summary>
        </member>
        <member name="F:DeserokUtils.Features.EphemeralMarks.EphemeralMarksFeature.font">
            <summary>
            ⚠ Constructed here but it builds nothing until a tag is actually drawn -- the atlas is created
            on first use, and the tag is off by default.
            </summary>
        </member>
        <member name="F:DeserokUtils.Features.EphemeralMarks.EphemeralMarksFeature.iconFont">
            <summary>
            ⚠ A second atlas, and it earns its keep: the glyphs have to be rasterised at the size they are
            drawn for exactly the reason the tag did. Built lazily like the other one, so a config using
            only the reticle and star never pays for it.
            </summary>
        </member>
        <member name="M:DeserokUtils.Features.EphemeralMarks.EphemeralMarksFeature.DrawOverlay">
            <summary>
            ⭐ Drawn on the BACKGROUND draw list rather than in a window -- no ImGui window to position,
            resize, click through or accidentally focus. It is an overlay, so it should not be a thing you
            can interact with.
            </summary>
        </member>
        <member name="M:DeserokUtils.Features.EphemeralMarks.EphemeralMarksFeature.Smooth(System.UInt64,System.Numerics.Vector2)">
             <summary>
             A low-pass filter on the screen position.
            
             ## ⚠⚠ This treats the symptom, and that is a deliberate concession
            
             Both position sources were tried -- the logical `IGameObject.Position` and the render transform
             on the draw object -- and both jitter while the game's own nameplate on the SAME character is
             steady. Rounding to whole pixels then made it dramatically worse, which is the useful clue:
             rounding cannot create motion, only convert sub-pixel motion into whole-pixel snapping. So the
             position genuinely oscillates by under a pixel and anti-aliasing was hiding it.
            
             ⭐ The most likely cause is one this code cannot reach: an overlay drawn at present time reads
             a transform at a different point in the frame than the game used to rasterise the character.
             The nameplate is steady because the game draws it with the matching transform. Nothing
             available from outside fixes that, so the honest options were "leave it jittering" or "filter
             it", and filtering wins.
            
             ⚠ FRAMERATE-INDEPENDENT, via the exponential form rather than a fixed lerp factor. A plain
             `lerp(a, b, 0.3f)` smooths three times harder at 30fps than at 120 -- so it would feel
             different on each of the three machines this is for, which is the bug one layer up from the one
             being fixed.
             </summary>
        </member>
        <member name="F:DeserokUtils.Features.EphemeralMarks.EphemeralMarksFeature.MotionPixels">
            <summary>Delta at which the motion is treated as entirely real and smoothing stops.</summary>
        </member>
        <member name="F:DeserokUtils.Features.EphemeralMarks.EphemeralMarksFeature.StillSeconds">
            <summary>⚠ Heavy -- for when the target is nearly still and every pixel of movement is noise.</summary>
        </member>
        <member name="F:DeserokUtils.Features.EphemeralMarks.EphemeralMarksFeature.MovingSeconds">
            <summary>⚠ Nearly nothing, so a camera pan is followed the frame it happens.</summary>
        </member>
        <member name="M:DeserokUtils.Features.EphemeralMarks.EphemeralMarksFeature.RenderPosition(Dalamud.Game.ClientState.Objects.Types.IGameObject)">
             <summary>
             Where the character is actually DRAWN this frame, not where the game logically thinks it is.
            
             ⚠⚠ THIS IS WHY THE MARKER JITTERED. `IGameObject.Position` is the logical position, updated on
             the game's tick; the model you are looking at is interpolated every frame. Anchoring to the
             logical value makes the marker step while the character glides underneath it -- most visible
             when they are moving, which in a Frontline is always. Nameplates do not jitter because they
             follow the render transform, which is what this reads.
            
             ⚠ Falls back to the logical position when there is no draw object -- someone loading in, or out
             of render range. A marker in very slightly the wrong place beats no marker.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.EphemeralMarks.EphemeralMarksFeature.UsesIcons">
             <summary>
             Whether anything currently configured needs the icon atlas.
            
             ⚠ Deliberately does NOT ask which people are marked -- it asks what is configured, so the
             answer is stable and the atlas does not get built and dropped as party members come and go.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.EphemeralMarks.EphemeralMarksFeature.StyleFor(System.String,System.Boolean)">
             <summary>
             Which glyph and colour this person gets: an override if they have one, otherwise the
             leader/member default with the shared colour.
            
             ⚠ A linear scan, in the draw loop, and that is fine here in a way the object-table scan was
             NOT. That one was ~600 entries with a string compare each, every frame. This is at most four
             markers against a handful of hand-typed overrides -- a couple of dozen compares -- and doing it
             per frame rather than at the 1 Hz resolve is what makes an edit in the tab show up instantly.
            
             ⚠⚠ Note what this function CANNOT do: it is only ever called for somebody already in the
             snapshot, so an override cannot add anyone. Keep it that way.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.EphemeralMarks.EphemeralMarksFeature.Section(System.String)">
            <summary>ImGui.SeparatorText does not exist in this binding version; this is the stand-in.</summary>
        </member>
        <member name="T:DeserokUtils.Features.EphemeralMarks.MarkFace">
            <summary>Which face to rasterise. ⭐ Same debounce and same lifecycle either way.</summary>
        </member>
        <member name="F:DeserokUtils.Features.EphemeralMarks.MarkFace.Axis">
            <summary>The game's own UI face, for the P2 tag.</summary>
        </member>
        <member name="F:DeserokUtils.Features.EphemeralMarks.MarkFace.Icons">
            <summary>Dalamud's Font Awesome, for the marker glyphs -- 1382 shapes, already shipped.</summary>
        </member>
        <member name="T:DeserokUtils.Features.EphemeralMarks.MarkFont">
             <summary>
             A font rasterised at the size it is actually drawn at.
            
             ## ⚠⚠ Why the tag was blurry while the shapes were crisp
            
             The shapes are vector geometry, so they are generated at whatever size is asked for. The tag was
             <c>AddText(ImGui.GetFont(), ImGui.GetFontSize() * scale, ...)</c> -- and a font is a **bitmap
             atlas rasterised at one fixed size**, so drawing it at any other size is a stretched image, not
             larger type. Both testers saw it immediately at 2x.
            
             ⚠ The resolution normalisation makes non-integer ratios the norm rather than the exception: at
             1080p the effective scale is <c>0.75 x slider</c>, so the default 2.0 asks for 1.5x -- every other
             output pixel sampling halfway between two texels. But even an exact 2x is soft under bilinear
             filtering, which is why deserok saw it too on 1440p. There is no scale factor that makes a
             resampled bitmap font sharp; the size has to be baked in.
            
             ⭐ deserok's framing, and it is the correct fix rather than a nicer resample: *"can we select a
             fontsize close to the scale? Like lets say 1x = 12pt, someone picks 2x, can we just move to 24pt"*.
            
             ⭐ The tag uses the game's own **Axis** face, which is what FFXIV sets its own UI in, so it reads
             as part of the game rather than as an overlay.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.EphemeralMarks.MarkFont.#ctor(DeserokUtils.Features.EphemeralMarks.MarkFace)">
             <summary>
             A font rasterised at the size it is actually drawn at.
            
             ## ⚠⚠ Why the tag was blurry while the shapes were crisp
            
             The shapes are vector geometry, so they are generated at whatever size is asked for. The tag was
             <c>AddText(ImGui.GetFont(), ImGui.GetFontSize() * scale, ...)</c> -- and a font is a **bitmap
             atlas rasterised at one fixed size**, so drawing it at any other size is a stretched image, not
             larger type. Both testers saw it immediately at 2x.
            
             ⚠ The resolution normalisation makes non-integer ratios the norm rather than the exception: at
             1080p the effective scale is <c>0.75 x slider</c>, so the default 2.0 asks for 1.5x -- every other
             output pixel sampling halfway between two texels. But even an exact 2x is soft under bilinear
             filtering, which is why deserok saw it too on 1440p. There is no scale factor that makes a
             resampled bitmap font sharp; the size has to be baked in.
            
             ⭐ deserok's framing, and it is the correct fix rather than a nicer resample: *"can we select a
             fontsize close to the scale? Like lets say 1x = 12pt, someone picks 2x, can we just move to 24pt"*.
            
             ⭐ The tag uses the game's own **Axis** face, which is what FFXIV sets its own UI in, so it reads
             as part of the game rather than as an overlay.
             </summary>
        </member>
        <member name="F:DeserokUtils.Features.EphemeralMarks.MarkFont.MinPx">
            ⚠ Bounds, because the size comes from a slider multiplied by a resolution ratio. The slider
            runs 0.4-2.5 and the ratio is 0.75 at 1080p through 1.5 at 4K, so the raw product spans roughly
            5px to 64px -- and a 5px atlas is unreadable while nothing needs more than this ceiling.
        </member>
        <member name="F:DeserokUtils.Features.EphemeralMarks.MarkFont.Settle">
            <summary>
            ⚠⚠ Rebuilds are DEBOUNCED, and that is not a micro-optimisation. Dragging the size slider
            walks through dozens of integer sizes, and each one would otherwise kick off a font atlas
            build -- the per-frame audit in DeserokUtils.md, in its most expensive possible form. Waiting
            for the number to settle means one build per adjustment instead of fifty.
            </summary>
        </member>
        <member name="M:DeserokUtils.Features.EphemeralMarks.MarkFont.Prepare(System.Single)">
             <summary>
             Ask for a size, and get back the size that should actually be drawn.
            
             ⭐ Returns the BUILT size once a handle exists, not the requested one, so the glyphs are always
             drawn 1:1 against their own rasterisation -- asking for 23.4px from a 23px atlas would put the
             blur straight back.
            
             ⚠ Returns the requested size while nothing is built yet. The first frames after switching tags
             on therefore look exactly like the old behaviour, which is correct: a soft tag beats no tag,
             and the atlas build is asynchronous.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.EphemeralMarks.MarkFont.TryLock">
            <summary>
            ⚠ Null while the atlas is still building, or if it failed. Callers fall back to the default
            font rather than skipping the tag.
            </summary>
        </member>
        <member name="M:DeserokUtils.Features.EphemeralMarks.MarkFont.Rebuild">
            <summary>
            ⚠ Built on FIRST USE, and only ever when the tag is actually switched on -- it is off by
            default, so nobody who never turns it on pays for a font atlas at all.
            </summary>
        </member>
        <member name="T:DeserokUtils.Features.EphemeralMarks.MarkShape">
            <summary>
            Which glyph to draw over somebody. Requested by Bunny in #dev-notes, 2026-08-22:
            *"Add different symbols for marks please :) Heart is needed^"*
            </summary>
            <remarks>
            ⚠ The numbers are persisted, so these are append-only. Reordering renames everybody's saved
            choices into whatever now sits at that index.
            </remarks>
        </member>
        <member name="F:DeserokUtils.Features.EphemeralMarks.MarkShape.Reticle">
            <summary>The bespoke reticle. ⭐ Kept vector because it is deliberately in FFXIV's own visual
            language, which no general-purpose icon set can imitate.</summary>
        </member>
        <member name="F:DeserokUtils.Features.EphemeralMarks.MarkShape.Star">
            <summary>The tuned five-point star. Kept vector for the same reason.</summary>
        </member>
        <member name="F:DeserokUtils.Features.EphemeralMarks.MarkShape.Icon">
            <summary>A Font Awesome glyph, chosen by codepoint. See the Icon fields on the config.</summary>
        </member>
        <member name="T:DeserokUtils.Features.EphemeralMarks.MarkShapes">
             <summary>
             Every marker glyph, in one place.
            
             ⭐ Two rules hold across all of them, and a new shape that breaks either will look wrong in a way
             that is hard to name afterwards:
            
             1. **Outlines, not solids.** A filled shape becomes a blob over a bright or busy background. The
                reticle's small filled tip is the deliberate exception -- it is what makes the point read at a
                glance.
             2. **Every stroke twice, dark then coloured.** Thin light lines vanish against snow, sand, and
                most of a Frontline.
            
             ⚠ Stroke weights scale with the marker but carry a floor. A sub-pixel line does not anti-alias
             into something faint -- it flickers as the marker moves, which is worse than being too thick.
             </summary>
        </member>
        <member name="F:DeserokUtils.Features.EphemeralMarks.MarkShapes.Popular">
            ⭐ A shortlist for the empty search, because opening a picker onto 1382 alphabetical entries
            starting with "AddressBook" is worse than six good ones. Typing searches everything.
        </member>
        <member name="F:DeserokUtils.Features.EphemeralMarks.MarkShapes.allIcons">
            ⚠ Built once. Enum.GetValues over 1382 entries per frame would be the per-frame audit again,
            in a tab nobody has open most of the time.
        </member>
        <member name="M:DeserokUtils.Features.EphemeralMarks.MarkShapes.GlyphPicker(System.String,DeserokUtils.Features.EphemeralMarks.MarkShape@,System.Int32@,System.Single)">
             <summary>
             Shape, plus a glyph picker when the shape is Icon.
            
             ⚠ The search is capped at 80 results and SAYS SO when it truncates. A silent cap reads as
             "that icon does not exist", which would send somebody looking for a bug that is not there.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.EphemeralMarks.MarkShapes.DrawGlyph(Dalamud.Bindings.ImGui.ImDrawListPtr,System.Int32,System.Nullable{Dalamud.Bindings.ImGui.ImFontPtr},System.Single,System.Numerics.Vector2,System.UInt32,System.Single)">
             <summary>
             A Font Awesome glyph with a shadow layer behind it -- the Honorific approach, and deserok's
             call after I spent a while tuning bezier curves: *"theres hearts, triangles, hell, all kinds of
             random shapes... can't we just do our own outline by making one glyph, and then adding a
             shadowlayer to it."*
            
             ⭐⭐ This replaces five hand-rolled outlines with 1382 properly drawn shapes, and it is strictly
             less code. The curve-fitting was the wrong instinct: the answer was already shipped in Dalamud.
            
             ## ⚠ The shape rule is AMENDED here, not broken
            
             Every other glyph is an outline because a filled shape becomes a blob over a bright background.
             Font Awesome icons are solid. But outlining was the MEANS -- the end is staying readable over
             snow and spell effects, and a solid glyph ringed in dark achieves that better than a thin
             stroke, which is why map markers in most games look like this.
            
             ⚠ Eight offsets, not four. Four leaves the diagonals bare and the halo visibly square at the
             corners; eight closes it for one more pass over two glyphs.
            
             ⚠ Falls back to the reticle while the atlas is still building, rather than drawing nothing. A
             marker that blinks out for a few frames after a settings change reads as a bug.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.EphemeralMarks.MarkShapes.DrawReticle(Dalamud.Bindings.ImGui.ImDrawListPtr,System.Numerics.Vector2,System.UInt32,System.Single)">
             <summary>
             An outlined diamond with a filled point and a small detached square above it.
            
             ⭐ Deliberately in the visual language of the game's own target reticle -- a marker that looks
             like it belongs reads faster than one that looks like a debug overlay.
            
             ⚠⚠ But not a colour a target reticle uses, and that is the point. A friend marker mistakable
             for "this is my current target" would be a worse misread than no marker at all. Same family,
             different signal.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.EphemeralMarks.MarkShapes.DrawStar(Dalamud.Bindings.ImGui.ImDrawListPtr,System.Numerics.Vector2,System.UInt32,System.Single)">
             <summary>
             A five-pointed star, the default for the party leader.
            
             ⭐ deserok's idea, and asymmetric in exactly the useful direction: from the leader's client
             everyone else is a diamond, while from Bunny's and Q's the leader is a star. They follow him,
             so "where is he" is the question they are actually asking -- and a distinct SHAPE answers it
             faster than a distinct colour, because shape survives peripheral vision and colour blindness.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.EphemeralMarks.MarkShapes.Stroke(Dalamud.Bindings.ImGui.ImDrawListPtr,System.Span{System.Numerics.Vector2},System.Single,System.Single,System.UInt32)">
            <summary>The two-pass stroke every closed shape shares. Dark underneath, colour on top.</summary>
        </member>
        <member name="T:DeserokUtils.Features.EphemeralMarks.ContentGroup">
            <summary>Which broad kind of content you are standing in, for the four grouped toggles.</summary>
        </member>
        <member name="T:DeserokUtils.Features.EphemeralMarks.MarkTracker">
             <summary>
             Remembers who you queued with, and finds them again once you are inside.
            
             ## ⭐⭐ ONE READ, AT ONE EVENT. No polling at all.
            
             **The game freezes your party the moment you queue.** deserok: *"you cannot change a party after
             you join the queue, adding a party member cancels the queue, and removing a party member does the
             same."* So there is nothing to poll for -- the party between queueing and entering is immutable,
             and a single read at the pop is the entire mechanism.
            
             ⭐ That also makes the event choice low-risk rather than critical. Since the party cannot change
             in that window, a snapshot at queue-join, at the pop, or at accept would all be the SAME DATA.
             The hook only has to exist; it does not have to be the precise right moment.
            
             ⭐ And a rejected queue -- somebody did not click accept -- simply re-pops and overwrites with
             identical data. No rejection handling, no withdrawal handling, no staleness. The states that would
             need cleaning up are states where the data is the same anyway.
            
             ⚠⚠ THE FIRST VERSION POLLED, AND IT WAS WRONG TWICE OVER. It rebuilt the party list every frame
             -- ~480 string allocations a second for data that changes once an hour, the per-frame audit
             committed again in a file whose comments cite it -- and it was solving a problem the game already
             solves. deserok caught the polling before it ever ran, then removed the need for it entirely:
             *"what we want is the thinnest version of each feature."*
            
             ## ⚠⚠ Names and home worlds. Never content or account ids.
            
             Nothing is persisted. That keeps this a HUD aid -- it shows where somebody standing in front of
             you is, which the party list already tells you -- rather than anything that could know where a
             person is later. See the plugin-safety section of ORIENTATION.md.
             </summary>
        </member>
        <member name="F:DeserokUtils.Features.EphemeralMarks.MarkTracker.PopAddon">
            <summary>
            The duty-ready dialog. ⚠ If this addon name is wrong the snapshot simply never fills and the
            tab says "came in alone" -- which is why <see cref="P:DeserokUtils.Features.EphemeralMarks.MarkTracker.Captured"/> is logged loudly when it fires.
            </summary>
        </member>
        <member name="P:DeserokUtils.Features.EphemeralMarks.MarkTracker.Idle">
            <summary>Why marks are not showing, or null if they are.</summary>
        </member>
        <member name="M:DeserokUtils.Features.EphemeralMarks.MarkTracker.OnPop(Dalamud.Game.Addon.Lifecycle.AddonEvent,Dalamud.Game.Addon.Lifecycle.AddonArgTypes.AddonArgs)">
             <summary>
             The queue popped. Read the party once.
            
             ⚠ Overwrites unconditionally, including on a re-pop after somebody declined. That is correct
             rather than lazy: the party cannot have changed, so the new read is identical.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.EphemeralMarks.MarkTracker.OnTerritoryChanged(System.UInt32)">
            <summary>
            ⭐ Event-driven, so nothing asks "where am I" per frame. The group is recomputed exactly when
            the answer can change.
            </summary>
        </member>
        <member name="M:DeserokUtils.Features.EphemeralMarks.MarkTracker.Tick">
             <summary>
             ⚠ Does nothing at all unless marks are actually showing -- one bool test per frame outside
             tracked content. The resolve inside is 1 Hz, and only runs where the feature is live.
            
             ⭐ Re-evaluated each call so a checkbox flipped in the tab takes effect without zoning.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.EphemeralMarks.MarkTracker.Resolve">
             <summary>
             Name + world -> object id, once a second while active.
            
             ⚠⚠ NOT PER FRAME. ~600 object-table entries with a string compare each, every frame, is
             exactly the shape the per-frame audit in DeserokUtils.md exists to catch. The draw path only
             looks objects up by id.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.EphemeralMarks.MarkTracker.Tag(System.String,System.UInt32)">
             <summary>
             A Helldivers-style identifier -- first initial plus party slot, so "P2" rather than
             "Peachi Bunni".
            
             ⭐ deserok's call after seeing a full name floating over her head: *"we do not want or need a
             name over their head."* He is right, and it is not only clutter -- a name is variable width, so
             a row of marked players jitters as they move, while two glyphs never do.
            
             ⚠ The slot is read LIVE rather than captured, so it matches the party list you are looking at.
             The instance rebuilds your party, so the number at the queue pop is not the number you see
             inside. Costs eight comparisons per marked player, once a second, on a pass that is already
             walking the object table.
            
             ⚠ Falls back to the initial alone when they are not in your current party -- which happens
             whenever the content splits a premade, and is precisely when the marker matters most. Better a
             bare "P" than a confidently wrong number.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.EphemeralMarks.MarkTracker.GroupFor(System.UInt32)">
            <summary>
            ⭐ Classified from `TerritoryType.TerritoryIntendedUse` -- a RULE rather than a zone list, so a
            new field operation sorts itself provided it reuses an existing value. Memoised, though this is
            now only called on a territory change anyway.
            </summary>
        </member>
        <member name="T:DeserokUtils.Features.FateWatch.FateTracker">
             <summary>
             Watches the live FATE table for tracked names appearing, and predicts the next spawn.
            
             ⚠⚠ THE CONSTRAINT THAT SHAPES ALL OF THIS: IFateTable only contains FATEs that are ACTIVE RIGHT
             NOW. A FATE spawning in ten minutes is not in it and cannot be read. So "ten minutes away" is
             never observed -- it is derived from when the last one appeared plus a cycle length, and it is
             only as trustworthy as that anchor.
            
             Which means: no prediction at all until one spawn has been seen. That is a real limitation, not
             a bug, and the UI says so rather than showing a confident countdown built on nothing.
             </summary>
        </member>
        <member name="F:DeserokUtils.Features.FateWatch.FateTracker.firedAlerts">
            <summary>Alert thresholds already fired for the current prediction, so each fires once.</summary>
        </member>
        <member name="F:DeserokUtils.Features.FateWatch.FateTracker.lastPlace">
             <summary>
             Where we were last time we looked, so a move can be noticed at all.
            
             ⚠⚠ An anchor is a statement about ONE instance. The pots are thirty minutes apart but NOT
             pegged to the clock, so a fresh instance starts a fresh ring from whenever it started -- which
             makes a carried-over anchor not stale-but-close, but meaningless. Null means "have not looked
             yet", which is also the state after a plugin reload.
             </summary>
        </member>
        <member name="F:DeserokUtils.Features.FateWatch.FateTracker.PollEvery">
            <summary>
            Polling once a second is plenty for something on a half-hour cycle, and keeps this off the
            per-frame path entirely.
            </summary>
        </member>
        <member name="M:DeserokUtils.Features.FateWatch.FateTracker.CheckPlace">
             <summary>
             Notice that the anchors no longer belong to where we are, and drop them.
            
             ⭐ Polled off the existing one-second tick rather than hooking IClientState.TerritoryChanged,
             because that event covers only ONE of the three ways an anchor outlives its instance. It does
             not fire for a plugin reload, and it does not fire for logging in somewhere else -- and both
             of those leave a previous instance's anchor sitting in the config, which is exactly the
             "still calling out pots while stood in Ul'dah" symptom. One comparison catches all three, and
             there is no event to unsubscribe in Dispose.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.FateWatch.FateTracker.DropAnchorsNotAt(System.UInt32,System.UInt32,System.String)">
             <summary>
             Drop every anchor that was not made at this exact place.
            
             ⭐ Keeps MeasuredIntervals. How long the cycle is, is evidence about the FATEs themselves and
             holds in every instance; WHEN the last one popped does not. Those two are the reason the
             anchor and the measurement were separate things to begin with.
            
             ⭐ Free side effect worth naming: RecordSpawn measures a gap against the previous anchor, so
             clearing it also stops the first spawn after a zone change from contributing a garbage
             interval measured across two different instances.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.FateWatch.FateTracker.AnchoredAt(System.String,System.UInt32,System.UInt32)">
            <summary>Whether this FATE's anchor was made at the given place. Territory 0 matches nothing.</summary>
        </member>
        <member name="M:DeserokUtils.Features.FateWatch.FateTracker.StampPlace(System.String)">
            <summary>Record WHERE an anchor was made, which is what lets it be retired later.</summary>
        </member>
        <member name="M:DeserokUtils.Features.FateWatch.FateTracker.AnchorManually(System.String,System.Double)">
             <summary>
             Anchor the cycle by hand, for what deserok already does manually: see one running, or ask
             shout chat. If someone says it popped six minutes ago, that is the same information the
             tracker would have got by watching -- there is no reason to make him wait for the next one
             just because the plugin was not looking.
            
             ⚠ Does NOT contribute to MeasuredIntervals. A number relayed through a stranger and a
             remembered "about six minutes" are not the same quality of evidence as an observed spawn,
             and letting them into the measurement would quietly corrupt the thing that corrects the
             assumption.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.FateWatch.FateTracker.AnchorForward(System.String,System.Double)">
             <summary>
             Anchor FORWARD: 'the next one is in N minutes'.
            
             ⭐ Covers the two things actually known in the wild, neither of which is an elapsed time.
             The wiki rule -- a fresh instance spawns its first pot ten minutes in -- and shout chat
             saying 'north in 12'. Expressing either through the elapsed-time anchor means doing modular
             arithmetic in your head while the content is running, which nobody does correctly.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.FateWatch.FateTracker.MinutesUntilNext(System.String)">
            <summary>
            Minutes until the next predicted spawn, or null when there is no anchor to predict from.
            ⚠ Null and zero are different answers and callers must not conflate them: one means "no
            idea", the other means "now".
            </summary>
        </member>
        <member name="M:DeserokUtils.Features.FateWatch.FateTracker.NextInRotation">
             <summary>
             The next member of the rotation, whichever FATE it is.
            
             ⭐ This is the question actually being asked. "When is Daylight Pottery" matters far less
             than "when is the next pot, and which side do I run to" -- and the rotation answers that from
             a single observation of ANY member, rather than needing to have seen each one separately.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.FateWatch.FateTracker.RotationIn(System.UInt32)">
             <summary>
             The rotation for a territory, or null if that zone has none.
            
             ⭐ Territory is how a rotation is FOUND now, rather than a filter applied afterwards. The
             Occult Crescent's two zones run two different pot pairs on independent rings, and a single
             flat list made that ring four long -- halving every prediction in both zones at once.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.FateWatch.FateTracker.CurrentRotation">
            <summary>The rotation for wherever the player is standing, or null.</summary>
        </member>
        <member name="M:DeserokUtils.Features.FateWatch.FateTracker.RotationOf(System.String)">
             <summary>
             The rotation a FATE belongs to, by name.
            
             ⚠⚠ Looked up by MEMBERSHIP, never by where the player is. A measured interval for Daylight
             Pottery is a fact about North Horn's ring and must be divided by North Horn's length --
             even while standing in South Horn. Using the current zone's length here would convert a
             measurement using a ring it was never part of.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.FateWatch.FateTracker.RotationLength(DeserokUtils.FateRotation)">
             <summary>
             How many slots a ring has, i.e. how many cycles before the SAME FATE comes round again.
            
             ⭐ The old version derived this from the single flat tracked list, and the note here warned
             that "what I track" and "what is in the rotation" were two ideas sharing one list -- to be
             split the moment there were two rotations. There are two, and this is that split.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.FateWatch.FateTracker.EffectivePerFateCycle(System.String)">
            <summary>The real gap between two spawns of the SAME fate, which is what a person means.</summary>
        </member>
        <member name="M:DeserokUtils.Features.FateWatch.FateTracker.EffectiveCycle(System.String)">
            <summary>
            The configured cycle, unless enough real intervals have been measured to disagree with it.
            ⭐ Median rather than mean: one missed observation produces a double-length gap, and a mean
            would let that single outlier drag every prediction late.
            </summary>
        </member>
        <member name="M:DeserokUtils.Features.FateWatch.FateTracker.Soonest">
            <summary>
            The tracked FATE due soonest, with its label, or null when nothing can be predicted.
            Used by the server bar, which has room for exactly one thing.
            </summary>
        </member>
        <member name="M:DeserokUtils.Features.FateWatch.FateTracker.IsTracked(System.String)">
            <summary>Whether any rotation, in any zone, has this FATE as a member.</summary>
        </member>
        <member name="M:DeserokUtils.Features.FateWatch.FateTracker.LabelFor(System.String)">
             <summary>
             The server-bar label for a FATE, from its OWN rotation. Empty when it has none.
            
             ⚠ Empty is a perfectly good answer, not a missing value -- South Horn has no labels yet
             because nobody has recorded where its two pots spawn. Every caller already renders a blank
             label as "no suffix".
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.FateWatch.FateTracker.ActiveFates">
            <summary>Everything currently in the table, for the discovery command.</summary>
        </member>
        <member name="T:DeserokUtils.Features.FateWatch.FateWatchFeature">
             <summary>
             Warn before a cyclic FATE spawns. Built for Occult Crescent's pot FATEs, which run on a fixed
             rotation that the game surfaces nowhere.
            
             ⚠⚠ The whole design is shaped by one constraint: the FATE table holds only what is ACTIVE. A
             spawn ten minutes out cannot be observed, so it is predicted from the last one seen. No anchor,
             no prediction -- and the UI says "not seen yet" rather than inventing a countdown.
             </summary>
        </member>
        <member name="F:DeserokUtils.Features.FateWatch.FateWatchFeature.lastDtr">
             <summary>
             ⚠⚠ The bar is throttled, and it was not. UpdateDtr ran EVERY FRAME: Soonest() re-derived the
             prediction, and BuildTooltip allocated a fresh SeString with a line per tracked FATE. Sixty
             times a second, to display a number in whole minutes that changes once a minute.
            
             ⭐ Matched to the tracker's own 1s poll, because nothing can change between polls anyway --
             the bar was redrawing from data that provably had not moved.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.FateWatch.FateWatchFeature.UpdateDtr">
             <summary>
             The server bar shows the soonest tracked FATE, or nothing at all.
            
             ⚠ Hidden rather than showing a placeholder when there is no prediction. A bar entry reading
             "--" is a thing you learn to ignore, and once ignored it is no longer a bar entry.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.FateWatch.FateWatchFeature.FindMember(System.String)">
             <summary>
             Find a FATE by name across EVERY rotation, not just this zone's.
            
             ⚠ Deliberately not scoped to the current territory: anchoring by hand from shout chat is
             exactly the case where you might be setting up a zone you are about to travel to.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.FateWatch.FateWatchFeature.DrawRotation(DeserokUtils.FateRotation,System.Boolean)">
             <summary>
             One rotation's members, timers and measured cycles.
            
             ⚠ The ORDER of the rows is the ring order, and it is load-bearing -- it decides which FATE
             comes next. The up/down buttons exist because the order cannot be inferred: the plugin only
             ever sees one member at a time, so it can never observe the ring itself.
             </summary>
        </member>
        <member name="T:DeserokUtils.Features.FcBuffs.ActivationStep">
            <summary>Where an activation attempt currently is. One attempt at a time, ever.</summary>
        </member>
        <member name="T:DeserokUtils.Features.FcBuffs.FcActionActivator">
             <summary>
             Performs one window session's worth of activations, one step per tick.
            
             ⚠⚠ A step machine rather than a function, because every step waits on the game: an addon does
             not exist the frame after it is asked for, and the confirmation dialog does not exist the frame
             after the row is fired. Written as straight-line code with sleeps it would either race the UI or
             block the render thread, and the first is the one that fails intermittently and unreproducibly.
            
             ⚠⚠ Replays a RECORDED payload. The two callbacks below were captured from deserok clicking the
             real buttons on 2026-08-07, not derived:
                 FreeCompany        FireCallback [Int=0, UInt=4]              (select the Actions tab)
                 FreeCompanyAction  FireCallback [Int=1, UInt=row]            (pick the row -> opens a menu)
                 ContextMenu        FireCallback [Int=0, Int=0, UInt=0, _, _] (item 0: Execute Action)
                 SelectYesno        FireCallback [Int=0]                      (yes)
            
             ⚠ The game ALSO fires a ContextMenu callback carrying cursor coordinates when the menu opens.
             That one is the game's, not ours -- only the selection above gets replayed.
             Nothing here was guessed, and if any of it stops working the recorder is how it gets re-derived.
            
             ⭐ The tab value is 4, and the tabs on screen read Topics, Members, Rank, Actions, Activity,
             Info -- which would make Actions 3. The obvious guess was wrong, and it was wrong in the
             direction that silently opens a different panel rather than failing.
             </summary>
        </member>
        <member name="F:DeserokUtils.Features.FcBuffs.FcActionActivator.StepTimeout">
            <summary>⚠ Every step has one. A machine that can wait forever is a machine that will.</summary>
        </member>
        <member name="F:DeserokUtils.Features.FcBuffs.FcActionActivator.SettleTicks">
             <summary>
             Ticks to wait between steps.
            
             ⚠ Not for stealth -- the UI genuinely needs frames to build an addon, and firing at one that
             is present but half-constructed is how a callback lands somewhere unintended.
             </summary>
        </member>
        <member name="F:DeserokUtils.Features.FcBuffs.FcActionActivator.queue">
            <summary>Buffs still to do after the current one. Worked through without reopening anything.</summary>
        </member>
        <member name="P:DeserokUtils.Features.FcBuffs.FcActionActivator.Completed">
            <summary>What actually went up this run, for the caller to report on.</summary>
        </member>
        <member name="F:DeserokUtils.Features.FcBuffs.FcActionActivator.weOpenedIt">
             <summary>
             Whether WE opened the FC window, and therefore owe it a close.
            
             ⚠ Not unconditional. /fcbuffs now is often run with the window already open by hand, and
             closing something the user opened is the plugin reaching past what it was asked to do.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.FcBuffs.FcActionActivator.Begin(System.Collections.Generic.IEnumerable{System.String})">
             <summary>
             Starts one window session covering every buff in <paramref name="wanted"/>.
            
             ⭐ deserok's shape, and it is better than one-window-per-buff for more than tidiness: closing
             and reopening between buffs meant the next Show() raced the previous window's teardown, and
             the addon pointer went null between being checked and being used. One session cannot race
             itself.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.FcBuffs.FcActionActivator.TickExecuting">
             <summary>
             Picking a row opens a CONTEXT MENU at the cursor -- Execute Action, then Discard Action --
             rather than going straight to a confirmation.
            
             ⚠⚠ This step did not exist in the first implementation, because the recorder's allowlist did
             not match "ContextMenu" and so the step was absent from the recording. It surfaced as
             "step Confirming timed out" with a menu left open under the cursor.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.FcBuffs.FcActionActivator.TickClosing">
             <summary>
             Puts the FC window back.
            
             ⚠⚠ NOT just tidiness. Leaving it open kept an Occupied condition set, which reset the settle
             clock on every check -- so the FIRST buff refreshed and the second never became eligible, for
             as long as the window stayed up. The plugin was blocking itself with its own side effect, and
             from outside that is indistinguishable from "it only does one".
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.FcBuffs.FcActionActivator.CloseWindow">
             <summary>
             Closes the FC window through the agent that owns it.
            
             ⚠⚠ NOT FireCloseCallback on the FreeCompany addon. That closed the ACTIONS PANEL and left the
             parent frame standing -- an empty window with the tabs still listed and nothing in the body.
             The addon is a shell hosting a child, so closing "the window" that way closes the wrong half.
            
             ⭐ Show() opened it, so Hide() closes it. Pairing the call with the one that opened it is the
             version that cannot be half-right.
             </summary>
        </member>
        <member name="T:DeserokUtils.Features.FcBuffs.FcActionRecorder">
             <summary>
             Records what the game does when YOU switch an FC buff on by hand, so the plugin can later do
             the same thing without anybody having guessed at it.
            
             ⚠⚠ This exists because the alternative is inventing a payload. Firing a made-up callback into
             a window is not a thing that fails loudly -- it either does nothing, or it presses something
             else. Neither reports itself, and both look exactly like "the feature is broken". So the button
             press gets observed once, from a real click, and replayed verbatim.
            
             ⚠⚠ TWO SOURCES, because the cheap one turned out not to carry the answer. AddonLifecycle gives
             the UI event -- which named both windows and found the confirmation dialog -- but its EventParam
             is NOT the row index: measured 2026-08-07, clicking six different buffs in the list produced
             eventParam=2 every single time. It identifies the KIND of event, not the item. The row index is
             in the AtkValues handed to FireCallback, so that gets hooked too.
             </summary>
        </member>
        <member name="F:DeserokUtils.Features.FcBuffs.FcActionRecorder.Expiry">
            <summary>Stops on its own. An armed recorder logging every UI event is not a thing to leave running.</summary>
        </member>
        <member name="F:DeserokUtils.Features.FcBuffs.FcActionRecorder.MaxEntries">
            <summary>⚠ A hard cap, because mouse-over alone produces events faster than anyone can read.</summary>
        </member>
        <member name="F:DeserokUtils.Features.FcBuffs.FcActionRecorder.AlwaysNoisy">
             <summary>
             Addons that are NEVER logged. Everything else is.
            
             ⚠⚠ A BLOCKLIST, and it took two identical failures to get here. Filtering by what to INCLUDE
             means naming the addons involved before you know what they are -- which is the entire thing
             the recorder exists to find out. It hid the confirmation dialog first (SelectYesno has no
             "compan" in it), and then, after "selectyesno" was added, it hid the context menu (ContextMenu
             has none of the three). Both times the recording looked complete. Both times a step was
             missing, and the second one was only found by an activation failing in front of deserok.
            
             ⚠ Unfiltered is still not an option: the very first run burned its whole 300-event budget
             before the FC window opened, 182 of those from the limit-break gauge in a city where it is
             never drawn. But the noisy addons are KNOWN and finite, which the interesting ones are not --
             so exclude what has been measured as noise and keep everything else.
             </summary>
        </member>
        <member name="F:DeserokUtils.Features.FcBuffs.FcActionRecorder.filters">
            <summary>Optional allowlist. Empty means "everything except <see cref="F:DeserokUtils.Features.FcBuffs.FcActionRecorder.AlwaysNoisy"/>".</summary>
        </member>
        <member name="T:DeserokUtils.Features.FcBuffs.FcActionRecorder.FireCallbackDelegate">
            <summary>
            Signature taken from the InteropGenerator delegate in the installed FFXIVClientStructs, NOT
            from memory: AtkUnitBase.FireCallback(UInt32 valueCount, AtkValue* values, Boolean close).
            If a patch changes it, this fails to compile instead of silently mis-reading arguments.
            </summary>
        </member>
        <member name="F:DeserokUtils.Features.FcBuffs.FcActionRecorder.installed">
             <summary>
             ⚠⚠ Nothing is installed until the recorder is ARMED. Both of these sit in genuinely hot
             paths -- the listener fires on every addon event in the game, the hook on every UI callback --
             and they existed to be used for about thirty seconds, twice. Leaving them enabled costs a
             branch on every one of those, permanently, for a diagnostic that is almost never running.
            
             ⭐ The cheapest branch is the one that is not there.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.FcBuffs.FcActionRecorder.Toggle(System.String)">
            <param name="nameFilter">
            Comma-separated substrings an addon's name must contain. Empty records everything that
            survives the known-noise blocklist.
            </param>
        </member>
        <member name="M:DeserokUtils.Features.FcBuffs.FcActionRecorder.Tick">
            <summary>Called from the feature's tick so an armed recorder cannot outlive its window.</summary>
        </member>
        <member name="M:DeserokUtils.Features.FcBuffs.FcActionRecorder.FireCallbackDetour(FFXIVClientStructs.FFXIV.Component.GUI.AtkUnitBase*,System.UInt32,FFXIVClientStructs.FFXIV.Component.GUI.AtkValue*,System.Boolean)">
            <summary>
            The payload the game actually sends. This is the thing worth replaying -- everything else in
            this class is context around it.
            </summary>
        </member>
        <member name="T:DeserokUtils.Features.FcBuffs.FcBuffPolicy">
             <summary>
             Decides WHETHER and WHAT to activate. The activator decides how.
            
             ⚠⚠ Everything here keys off PRESENCE, never duration. The status list's RemainingTime is a fixed
             30 for FC buffs and the agent's timers need an age subtracted -- so a design that asked "how long
             is left" would have had two ways to be wrong and no way to notice. "Is it on" has one answer and
             the game states it directly.
             </summary>
        </member>
        <member name="F:DeserokUtils.Features.FcBuffs.FcBuffPolicy.SettleTime">
             <summary>
             How long everything must stay quiet before the status list is believed.
            
             ⚠⚠ NOT politeness -- correctness. The status list is empty or partial right after a login and
             across every loading screen, so a naive comparison sees every zone change as "the buff just
             dropped": a false alert AND a spurious activation, at the exact moments they are least
             welcome. This is the same shape as FateWatch's anchor surviving a zone change, and the fix is
             the same one: do not trust a reading taken where it cannot be true.
             </summary>
        </member>
        <member name="F:DeserokUtils.Features.FcBuffs.FcBuffPolicy.RetryCooldown">
            <summary>⚠ One attempt per buff per this long. A retry loop is what turns one press a day into traffic.</summary>
        </member>
        <member name="F:DeserokUtils.Features.FcBuffs.FcBuffPolicy.lastSeenActive">
            <summary>
            What was up last time the world was trustworthy. Null until the first settled reading, which
            is what stops the very first tick after a login from reading as a drop.
            </summary>
        </member>
        <member name="P:DeserokUtils.Features.FcBuffs.FcBuffPolicy.Settled">
            <summary>True when the game is in a state where opening a window is reasonable.</summary>
        </member>
        <member name="P:DeserokUtils.Features.FcBuffs.FcBuffPolicy.JustDropped">
            <summary>Buffs that dropped since the last settled reading. Consumed by the caller.</summary>
        </member>
        <member name="M:DeserokUtils.Features.FcBuffs.FcBuffPolicy.OnHomeWorld">
             <summary>
             Whether we are on the home world.
            
             ⚠⚠ FC BUFFS DO NOT APPLY WHILE VISITING ANOTHER WORLD -- the statuses simply go away. To a
             presence-only design that is indistinguishable from "they ran out", so the plugin cheerfully
             tried to refresh them, over and over, for the whole trip.
            
             ⭐ It costs nothing, checked with deserok: the FC window cannot even be OPENED while away, so
             every attempt died at the Opening step and no stock was ever spent. What it produced was a
             timeout error in chat every ten minutes -- a plugin loudly reporting a broken step, for a
             situation that is completely normal and about which nothing was wrong.
            
             ⚠ Which is its own kind of bad: an error that fires when nothing is broken teaches you to
             ignore the errors that mean something.
            
             ⭐ Grouped with the loading-screen check rather than treated as its own refusal, on purpose:
             being away from home is not a place where the status list means what it usually means. So the
             whole picture is dropped, exactly like a zone transition -- which also stops ARRIVING on
             another world from reading as "the buff just dropped" and firing a toast at you.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.FcBuffs.FcBuffPolicy.InSafePlace">
            <summary>Whether this place is on the safe list, by name rather than by number.</summary>
        </member>
        <member name="M:DeserokUtils.Features.FcBuffs.FcBuffPolicy.Observe">
            <summary>
            Re-reads the world. Call once a tick; it maintains the settle clock and the drop transitions.
            </summary>
        </member>
        <member name="F:DeserokUtils.Features.FcBuffs.FcBuffPolicy.lastObserved">
             <summary>
             The active set from the most recent <see cref="M:DeserokUtils.Features.FcBuffs.FcBuffPolicy.Observe"/>.
            
             ⚠ Reused rather than recomputed. Observe and AllToActivate ran back to back and each scanned
             the status list independently to build the identical set -- the same duplicated-work mistake
             as calling ActiveFcBuffs inside a LINQ predicate, one level up.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.FcBuffs.FcBuffPolicy.AllToActivate">
            <summary>
            Every wanted buff that is missing and has not been tried recently. Empty when there is
            nothing to do.
            </summary>
        </member>
        <member name="T:DeserokUtils.Features.FcBuffs.FcAction">
            <summary>One row of the CompanyAction sheet -- an FC buff that can be switched on.</summary>
        </member>
        <member name="M:DeserokUtils.Features.FcBuffs.FcAction.#ctor(System.UInt32,System.String,System.UInt32,System.UInt32,System.Boolean,System.Byte)">
            <summary>One row of the CompanyAction sheet -- an FC buff that can be switched on.</summary>
        </member>
        <member name="T:DeserokUtils.Features.FcBuffs.PlayerStatus">
            <summary>A status currently sitting on the player.</summary>
        </member>
        <member name="M:DeserokUtils.Features.FcBuffs.PlayerStatus.#ctor(System.UInt32,System.String,System.Single)">
            <summary>A status currently sitting on the player.</summary>
        </member>
        <member name="T:DeserokUtils.Features.FcBuffs.FcBuffReader">
            <summary>
            Everything this feature READS. Kept apart from the feature itself because "is the buff on" and
            "for how long" come from different sources -- the status list and the agent timers -- and they
            disagree in ways that matter.
            </summary>
        </member>
        <member name="F:DeserokUtils.Features.FcBuffs.FcBuffReader.knownActions">
             <summary>
             The FC actions the game knows about, from the CompanyAction sheet.
            
             ⭐ Read out of the running game rather than typed in. The names are what the buffs are matched
             by, and a name from memory or a wiki can be stale, localised, or simply a different thing --
             which is exactly how FateWatch shipped with two FATEs that never existed.
             </summary>
             <summary>
             ⚠⚠ CACHED, and not as a micro-optimisation. Sheet reads are per-row string extractions, and
             every one of these was being redone sixty times a second by the tab's draw call -- which is
             what tanked the framerate the moment the tab was opened. Excel sheets do not change during a
             session, so re-reading them per frame buys exactly nothing.
             </summary>
        </member>
        <member name="F:DeserokUtils.Features.FcBuffs.FcBuffReader.territoryCache">
            <summary>⚠ Memoised for the same reason: see <see cref="M:DeserokUtils.Features.FcBuffs.FcBuffReader.ResolveTerritories(System.String)"/>.</summary>
        </member>
        <member name="M:DeserokUtils.Features.FcBuffs.FcBuffReader.PlayerStatuses">
             <summary>
             Every status on the local player, named via the Status sheet.
            
             ⚠⚠ RemainingSeconds IS MEANINGLESS FOR FC BUFFS. Measured 2026-08-07: The Heat of Battle
             (365) and Reduced Rates (364) both read exactly 30s across three probes seventeen seconds
             apart, while the agent timers -- counting down correctly the whole time -- said ninety-seven
             minutes were left. A real 30-second countdown would have expired during the probe. It did not
             move, so it is not a countdown.
            
             ⭐ So this source answers WHICH buffs are on and nothing else. Duration comes from
             <see cref="M:DeserokUtils.Features.FcBuffs.FcBuffReader.RawTimers"/>. Reading a countdown off this list would have produced a plugin that
             announced an imminent expiry every thirty seconds, forever.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.FcBuffs.FcBuffReader.RawTimers">
             <summary>
             The three FC action timers held by the FreeCompany agent.
            
             ⚠⚠ TimeRemainingAtUpdate IS NOT TIME REMAINING. The struct is literally a snapshot plus an
             age: the game writes the remaining values when the agent last updated, then counts
             TimeSinceUpdate up from there. Reading the array on its own gives a number that is correct
             once and then never moves again -- a value that looks live, is not, and gets more wrong the
             longer the FC window stays shut.
            
             So the live value is (snapshot - age), which is what <see cref="M:DeserokUtils.Features.FcBuffs.FcBuffReader.LiveRemaining(System.UInt32,System.UInt32)"/> returns.
            
             ⭐ CONFIRMED 2026-08-07, and the subtraction is not academic. Across three probes: the
             snapshot sat unchanged at 5988 while the age climbed 158 -> 169, then the FC window was
             opened and the snapshot was rewritten to 5816 with the age reset to 3. Raw, that is a
             172-second cliff at the exact moment the window opened. Subtracted, it runs 5830 -> 5819 ->
             5813 -- continuous straight through the update, which is what says the model is right.
            
             ⭐ Units are SECONDS. The age advanced 11 over 11.36 s of wall clock.
            
             ⚠ THREE slots, and nothing here says which ACTION is in which slot -- the struct carries no
             action id. Slot 0 is not "Heat of Battle"; it is whatever is in slot 0. Identifying the buff
             is what the player's status list is for. Do not join these two by position.
            
             ⚠ An empty slot reads 0, so the count of non-zero slots is how many actions are running --
             which agreed with the status list (two of each) but is a cross-check, never the identity.
            
             ⚠⚠ THREE SLOTS IS NOT A LIMIT OF THREE. The game only permits TWO active company actions
             (deserok, 2026-08-07, who owns the FC). What the third slot is for is unknown and left that
             way rather than guessed at. The array length is a fact about how the client allocates, and
             reading a game rule out of it is the same mistake as deriving the FATE rotation length from
             how many entries happened to be in TrackedFates -- a container's size quietly became a fact
             about the world, and was wrong.
            
             ⭐ Nothing here depends on either number, and that is deliberate: presence comes from the
             status list, so the cap can be two, three or five without this code caring.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.FcBuffs.FcBuffReader.LiveRemaining(System.UInt32,System.UInt32)">
            <summary>
            Snapshot minus age, floored at zero. See the warning on <see cref="M:DeserokUtils.Features.FcBuffs.FcBuffReader.RawTimers"/> for why the
            subtraction is not optional.
            </summary>
        </member>
        <member name="M:DeserokUtils.Features.FcBuffs.FcBuffReader.FreeCompanyRank">
            <summary>
            The FC's rank, which is what decides how much of the action list is even shown.
            Zero if the proxy has no data yet.
            </summary>
        </member>
        <member name="F:DeserokUtils.Features.FcBuffs.FcBuffReader.ListStringArray">
             <summary>
             Where the FreeCompanyAction list's text lives, measured 2026-08-07.
            
             ⚠⚠ ALL THREE NUMBERS ARE OBSERVATIONS, NOT CONTRACTS. Array 58, the inactive list starting at
             entry 8, stride 2 -- derived from a single dump cross-referenced against six recorded clicks
             (rows 0-4 landed on entries 8,10,12,14,16). Nothing in the game data states any of them, and
             a patch can move all three.
            
             ⭐ Which is why nothing acts on these alone: <see cref="M:DeserokUtils.Features.FcBuffs.FcBuffReader.ReadListEntry(System.Int32)"/> is always used to
             confirm the row holds the expected NAME before that row is fired at. A wrong stride then
             becomes a refusal and a log line, instead of quietly consuming the wrong buff.
             </summary>
        </member>
        <member name="F:DeserokUtils.Features.FcBuffs.FcBuffReader.InactiveCountEntry">
             <summary>
             Entry 7 holds the inactive list's own count, as the "14/15" the window prints.
            
             ⭐⭐ THIS IS THE END OF THE LIST, and it is the only honest one. Scanning until an empty entry
             over-counts, because the array keeps stale rows past the live end and a ghost row names a real
             buff -- indistinguishable by content, which is why the earlier "stop at the first blank"
             scan read 8 Reduced Rates against 7 on screen. The game states the length; it does not have
             to be inferred.
            
             ⚠ The same shape sits in the ACTIVE section: entry 3 still named Reduced Rates II long after
             it expired, and what gave it away was entry 4 -- its duration -- being blank. Every part of
             this array remembers; only the counts say what is real.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.FcBuffs.FcBuffReader.ReadStringArray(System.Int32,System.Int32)">
            <summary>Reads one entry of a UI string array. Null when out of range or unreadable.</summary>
        </member>
        <member name="M:DeserokUtils.Features.FcBuffs.FcBuffReader.ReadListEntry(System.Int32)">
            <summary>The name shown at a given row of the INACTIVE action list, or null.</summary>
        </member>
        <member name="M:DeserokUtils.Features.FcBuffs.FcBuffReader.InactiveCount">
            <summary>
            How many rows of the inactive list are real, from the game's own "14/15" counter.
            Null when the window is shut or the entry does not parse.
            </summary>
        </member>
        <member name="M:DeserokUtils.Features.FcBuffs.FcBuffReader.RowsHolding(System.String)">
            <summary>Rows of the inactive list holding the given buff, in list order.</summary>
        </member>
        <member name="M:DeserokUtils.Features.FcBuffs.FcBuffReader.BestRowFor(System.String)">
             <summary>
             The best row to spend for a family: highest tier in stock, earliest row among equals.
            
             ⭐ "Highest available" rather than a configured tier, per deserok -- and it stays correct
             through the expansion plan, where the IIIs get loaded into the pool and simply start winning.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.FcBuffs.FcBuffReader.ContextMenuFirstItem(FFXIVClientStructs.FFXIV.Component.GUI.AtkUnitBase*)">
             <summary>
             Item 0 of the open context menu, read from the addon's OWN AtkValues. What the caller checks
             the menu's order against before firing an index at it.
            
             ⚠⚠ THE ONE DESTRUCTIVE MISTAKE AVAILABLE IN THIS FEATURE. Item 0 executes; item 1 DISCARDS,
             which destroys a bought action outright. Firing an index into a menu whose order is assumed
             is the single thing here that can lose something -- everything else fails by doing nothing.
            
             ⭐ And "the obvious value is right" has been wrong at every step of this feature: eventParam
             was not the row, the Actions tab was 4 and not 3, the list carried ghost rows, and the filter
             hid two whole steps. The order is checked because the track record says to check it.
            
             ⚠ Returns null when the menu text cannot be read at all, so an unreadable menu refuses
             rather than proceeds.
            
             ⚠⚠ Layout measured 2026-08-07: value 0 is the ITEM COUNT, and the labels start at value 8.
             Values past base+count are STALE -- a live 2-item menu still carried "View Company Profile",
             "Search for Item" and "Try On" from menus opened earlier in the session.
            
             ⭐⭐ THAT IS THE THIRD TIME THIS EXACT SHAPE HAS APPEARED: the inactive action list, the active
             action slots, and now this menu. Every one is a count field beside a buffer that never clears,
             and every one punishes reading the buffer without the count. It is the engine's habit, not a
             series of coincidences -- treat any UI array here as unbounded junk until a count says
             otherwise.
            
             ⚠ The previous version searched the UI STRING arrays for this text and never found it,
             because menu labels do not live there. What it did find was array 0 -- the chat log -- where
             it matched the word "Action" inside this feature's own error messages.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.FcBuffs.FcBuffReader.FindActionStrings">
             <summary>
             Hunts every string array the UI holds for entries that name a company action, and reports
             where they sit.
            
             ⭐ This is how the row index gets a MEANING. The callback carries an index and nothing else,
             and the list it indexes is an inventory with duplicates -- four copies of the same buff -- so
             the index cannot be derived from the sheet. The window's backing string array is the only
             thing that knows which row is which.
            
             ⚠ Searched rather than looked up by a known array id. Guessing the array is the same class of
             mistake as guessing the addon name, and a search that finds nothing is at least honest about
             finding nothing.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.FcBuffs.FcBuffReader.LoadedAddons">
             <summary>
             Every addon the game currently has loaded, by name.
            
             ⭐ The discovery step for "what is the FC window actually called". Guessing at addon names is
             the same mistake as guessing at FATE names, and it fails the same silent way: a listener
             registered against a name that does not exist never fires and never complains.
             </summary>
        </member>
        <member name="F:DeserokUtils.Features.FcBuffs.FcBuffReader.lastTerritory">
            <summary>
            Where the player is, as the game names it. Used to seed and to extend the "safe to open a
            menu here" list without anybody typing a territory id.
            </summary>
        </member>
        <member name="M:DeserokUtils.Features.FcBuffs.FcBuffReader.CurrentPlace">
            <summary>⚠ Cached on the territory id -- this is called from the draw loop, once per frame.</summary>
        </member>
        <member name="M:DeserokUtils.Features.FcBuffs.FcBuffReader.ResolveTerritories(System.String)">
             <summary>
             Resolve a place name to every territory id carrying it.
            
             ⭐ This is why the config stores NAMES, not numbers. A hardcoded id list is a page of figures
             nobody can check by eye and that goes stale on the next expansion -- and the one thing known
             about the next expansion is that it is coming. The sheet is the authority for the number; the
             name is the part a human can actually verify.
            
             ⚠ Returns every match, because a city is routinely several territories -- Ul'dah alone is
             Steps of Nald and Steps of Thal, and being in "the wrong half" of a city is not a state
             anybody thinks in.
             </summary>
             <summary>
             ⚠⚠ MEMOISED, and this one was the actual framerate bug. A miss walks the whole TerritoryType
             sheet extracting a place name per row, and the tab called it once per safe place -- twenty-two
             of them -- on every single frame. The answer is fixed game data, so it is computed once per
             name and kept.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.FcBuffs.FcBuffReader.NormaliseName(System.String)">
             <summary>
             Strips a trailing roman numeral, so an action and the status it applies can be compared.
            
             ⚠⚠ THE TIERS DO NOT SHARE A NAME. The FC owns "The Heat of Battle II"; the status it puts on
             the player is "The Heat of Battle", with no numeral (measured 2026-08-07 -- the window said
             II while the status list said neither II nor III). Exact-match name joining therefore fails
             for every buff actually in use.
            
             ⚠ It only looked like it worked because the CompanyAction sheet ALSO contains the un-numbered
             rank-1 rows, so the status matched one of those and the probe printed a cheerful "matches a
             company action". The join was landing on a different action than the one that was running.
            
             ⭐ Collapsing the tiers is not a loss here: the question being asked is "is this buff up",
             and for that, which tier is running does not matter.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.FcBuffs.FcBuffReader.TierOf(System.String)">
             <summary>
             The tier a company action name carries. No numeral is tier 1.
            
             ⭐ Tiers are what the config does NOT store. deserok has never once chosen to use a I over a
             II, and the IIIs he is saving get loaded on demand rather than held in the pool -- so "which
             tier" is not a preference, it is just "the best one currently in stock". Asking would be
             asking a question with one answer.
             </summary>
        </member>
        <member name="F:DeserokUtils.Features.FcBuffs.FcBuffReader.statusFamilies">
             <summary>
             Which buff families are currently up, normalised.
            
             ⚠⚠ ONE ENTRY PER STATUS, which the old shape got wrong. Joining statuses to actions by
             normalised name matches every tier -- so a single "The Heat of Battle" on the player produced
             three results (I, II and III) and the tab cheerfully reported three buffs running. The player
             has one status; the sheet has three rows that could have caused it. Those are different
             counts and only the first one is a fact about the world.
             </summary>
             <summary>
             Status id -> buff family, built once from the Status sheet.
            
             ⭐ So the hot path never touches a sheet or builds a string. The old version extracted a name
             for every status on the player, normalised it, and compared it against a set of action names
             -- thirty sheet lookups and thirty allocations, twice per poll, to answer a question that is
             really just "is this id one of ours". Ids are stable; resolve them once.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.FcBuffs.FcBuffReader.ActiveFamilies">
            <summary>Which buff families are up. Id lookups only -- no sheet reads, no allocations.</summary>
        </member>
        <member name="M:DeserokUtils.Features.FcBuffs.FcBuffReader.ActiveStatuses">
            <summary>The FC-buff statuses actually on the player -- one per status, for display.</summary>
        </member>
        <member name="M:DeserokUtils.Features.FcBuffs.FcBuffReader.ActiveFcBuffs">
             <summary>
             Statuses on the player that correspond to a known FC action.
            
             ⚠ Name matching, not id matching, and that is a compromise rather than a design. Nothing in
             the game data links a CompanyAction row to the Status row it applies, so the join is on the
             text -- normalised, per <see cref="M:DeserokUtils.Features.FcBuffs.FcBuffReader.NormaliseName(System.String)"/>. The probe prints both sides so a
             mismatch shows up as a mismatch rather than as "the buff is off".
            
             ⚠ Because tiers collapse, several action rows can match one status. Callers wanting "is this
             up" are fine; anything wanting "which exact row is running" is not answerable this way.
             </summary>
        </member>
        <member name="T:DeserokUtils.Features.FcBuffs.FcBuffsFeature">
            <summary>
            Keep the FC buffs running, because the failure is never "we did not know how" -- it is eight
            hours of grinding before anybody looks up.
            </summary>
        </member>
        <member name="F:DeserokUtils.Features.FcBuffs.FcBuffsFeature.lastCheck">
            <summary>⚠ Throttle. The framework tick is every frame; none of this needs to be.</summary>
        </member>
        <member name="F:DeserokUtils.Features.FcBuffs.FcBuffsFeature.stockLabels">
             <summary>
             Stock per buff family, for the tab's display only.
            
             ⚠⚠ Cached because the tab is a LOOP. Reading stock walks the UI string array per family, and
             doing that for every row on every frame is the same mistake as the sheet walks that tanked
             the framerate -- committed again, in the fix for it, about ten minutes later. Draw code is
             where innocent-looking reads become sixty-per-second reads.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.FcBuffs.FcBuffsFeature.FinishActivation">
            <summary>
            Reports the outcome once, then clears. Both of deserok's asked-for warnings live here.
            </summary>
        </member>
        <member name="M:DeserokUtils.Features.FcBuffs.FcBuffsFeature.ForceNow">
             <summary>
             Runs one activation immediately, ignoring the settle clock and the safe-place list.
            
             ⚠ The gates exist for the AUTOMATIC path, where nobody is watching. Asking for it by hand is
             somebody watching, so the gates would only be in the way -- but the dry-run flag still
             applies, because that one is about whether the payload is right, not about timing.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.FcBuffs.FcBuffsFeature.DumpStrings">
            <summary>
            Finds where the action list's text actually lives, so a row index can be turned into a name.
            </summary>
        </member>
        <member name="M:DeserokUtils.Features.FcBuffs.FcBuffsFeature.DumpValues">
             <summary>
             Dumps the FreeCompanyAction window's own AtkValues while it is open.
            
             ⭐ READS the list instead of predicting it. The predictor that used to live here was wrong in
             a way no amount of care would have fixed: it rebuilt the list from the CompanyAction sheet,
             one row per action, and the real window is an inventory of owned items with duplicates in it.
             The window already knows its own contents -- ask it.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.FcBuffs.FcBuffsFeature.ListAddons">
             <summary>
             Names every loaded addon, so the recorder's filter is set from what the game actually calls
             its windows rather than from a guess.
            
             ⚠ LOADED, not visible. The limit-break gauge is loaded and firing events in Ul'dah, where it
             is never drawn -- so this list is longer than the screen and that is not a bug in it.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.FcBuffs.FcBuffsFeature.OpenFcWindow">
             <summary>
             Opens the Free Company window through the agent that owns it.
            
             ⭐ Not a keybind and not a text command. The agent exposes Show() directly, so there is no
             key to be rebound, no command to be localised, and nothing to lose a race with.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.FcBuffs.FcBuffsFeature.Summary">
            <summary>
            What is on right now, one line per STATUS. Deliberately not FcBuffReader.ActiveFamilies,
            which collapses tiers to one entry per family and so cannot say WHICH tier you are carrying.
            </summary>
        </member>
        <member name="M:DeserokUtils.Features.FcBuffs.FcBuffsFeature.Probe">
            <summary>
            Dumps every source at once so they can be compared side by side rather than one guess at a
            time. The three questions it was written to settle are settled -- the measurements are on
            FcBuffReader.PlayerStatuses and FcBuffReader.RawTimers -- and it stays as the way to re-check
            them against a live game:
              1. do FC buffs appear in the player's status list, with a real countdown? (yes; no)
              2. do the agent's three timers agree with them once the age is subtracted? (yes)
              3. what unit are the agent timers in? (seconds)
            </summary>
        </member>
        <member name="T:DeserokUtils.Features.IfMouseover.ActionLookup">
             <summary>
             Action name -&gt; id, memoised, kept as two maps because <b>one name can be two actions</b>.
            
             ⚠ CastWatch has its own copy of this lookup and walks the whole Action sheet on every call. That
             is fine there -- /watch runs once when a macro arms -- and it is NOT fine here, because /ifmo
             runs on every press of every macro that uses it. Hence dictionaries built once.
            
             ⚠ Deliberately NOT extracted out of CastWatch. Unifying them means editing shipped, working code
             for tidiness, and the two have genuinely different needs. ⚠⚠ Note that CastWatch therefore does
             NOT have the PvP handling below -- `/watch` on a colliding name will arm the non-PvP row. Left as
             a known gap rather than fixed blind, because /watch's hook compares whatever id the game actually
             sent, so the failure mode there is different and needs its own measurement.
            
             ## ⚠⚠ 103 action names exist as BOTH a PvP and a non-PvP action
            
             Measured from the Action sheet, not assumed. And they are not variants of each other -- Guardian
             is the clearest case:
            
             <code>
             row 29066  PvP   range 20  targets a party member  30s   "Rush to a target party member's side"
             row 36920  PvE   range  0  SELF ONLY              120s   "Reduces damage taken by 40%"
             </code>
            
             ⚠ Picking the wrong one does not fail loudly. It validates the mouseover against the wrong
             action's rules and then reports a confident, wrong answer -- the same failure shape as the
             forgotten quotes, where the diagnostics look healthy and nothing happens.
            
             ⚠⚠ First-wins across the whole sheet is NOT a safe tiebreak here. For 102 of the 103 the non-PvP
             row has the lower id, so first-wins silently means "always the PvE one" -- and Guardian is the
             single exception, which is exactly the action that exposed the gap. Getting the right answer by
             accident on the one case somebody tested is worse than getting it wrong everywhere.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.IfMouseover.ActionLookup.Build">
             <summary>
             ⚠ Player actions only, same restriction CastWatch uses, so a name cannot silently bind to some
             internal ability that shares it.
            
             ⚠ Built on FIRST USE, not at load. The sheet walk is ~thousands of string extractions and
             nobody who never types /ifmo should pay for it -- the per-frame audit in DeserokUtils.md is
             what this is avoiding, one class earlier than usual.
             </summary>
        </member>
        <member name="T:DeserokUtils.Features.IfMouseover.ActionLookup.Resolved">
            <summary>What a name resolved to, and whether the choice was contested.</summary>
            <param name="Id">The action id to send. Never zero -- a name that matched nothing returns null instead.</param>
            <param name="Pvp">Which map it came out of, not which map was asked for.</param>
            <param name="Ambiguous">
            The name exists in both maps, so the PvP decision actually mattered. Traced when true, because
            this is the case where a silent wrong answer is possible.
            </param>
        </member>
        <member name="M:DeserokUtils.Features.IfMouseover.ActionLookup.Resolved.#ctor(System.UInt32,System.Boolean,System.Boolean)">
            <summary>What a name resolved to, and whether the choice was contested.</summary>
            <param name="Id">The action id to send. Never zero -- a name that matched nothing returns null instead.</param>
            <param name="Pvp">Which map it came out of, not which map was asked for.</param>
            <param name="Ambiguous">
            The name exists in both maps, so the PvP decision actually mattered. Traced when true, because
            this is the case where a silent wrong answer is possible.
            </param>
        </member>
        <member name="P:DeserokUtils.Features.IfMouseover.ActionLookup.Resolved.Id">
            <summary>The action id to send. Never zero -- a name that matched nothing returns null instead.</summary>
        </member>
        <member name="P:DeserokUtils.Features.IfMouseover.ActionLookup.Resolved.Pvp">
            <summary>Which map it came out of, not which map was asked for.</summary>
        </member>
        <member name="P:DeserokUtils.Features.IfMouseover.ActionLookup.Resolved.Ambiguous">
            <summary>
            The name exists in both maps, so the PvP decision actually mattered. Traced when true, because
            this is the case where a silent wrong answer is possible.
            </summary>
        </member>
        <member name="M:DeserokUtils.Features.IfMouseover.ActionLookup.Resolve(System.String,System.Boolean)">
             <summary>
             ⭐ <paramref name="preferPvp"/> comes from two places, either of which is sufficient: an
             explicit <c>/pvpac</c> verb, or the client reporting that you are in PvP.
            
             ⚠ Falls back to the other map when the preferred one has no such name, so this is never worse
             than the single-map version it replaced -- a PvE-only action named in a PvP zone still
             resolves, and the game refuses it exactly as it would have.
             </summary>
        </member>
        <member name="P:DeserokUtils.Features.IfMouseover.ActionLookup.InPvp">
             <summary>
             ⭐ Whether the client says you are in PvP, which is the default half of the preference above.
            
             ⚠ Uses <c>IsPvP</c> and NOT <c>IsPvPExcludingDen</c>, deliberately. The Wolves' Den Pier is
             flagged as a PvP zone by the game -- the only such zone with an ordinary overworld
             TerritoryIntendedUse -- and your hotbar swaps to the PvP bar while you stand in it. So the Den
             is precisely a place where a bare /ac means the PvP action, and excluding it would break the
             one zone people practise in.
             </summary>
        </member>
        <member name="F:DeserokUtils.Features.IfMouseover.ActionLookup.Verbs">
             <summary>
             The action commands that take an action name plus a placeholder, from the TextCommand sheet.
            
             ⚠⚠ <c>/pvpaction</c> being missing here is the gap deserok found: <c>/ifmo /pvpac "Guardian"
             {mo}</c> parsed no name at all, so it silently dropped to the weak presence check instead of
             validating range and line of sight.
            
             ⚠ Order is not load-bearing but the trailing space is: without it <c>/ac </c> would match
             inside <c>/action</c>. Every entry keeps it.
            
             ⚠ <c>/gaction</c> is deliberately absent -- general actions live in their own sheet and do not
             resolve to an Action row, so accepting the verb would mean parsing a name we then cannot look
             up. Better to not claim the verb than to claim it and degrade.
             </summary>
        </member>
        <member name="T:DeserokUtils.Features.IfMouseover.ActionLookup.ActionSpan">
            <summary>Where the action name sits in the line, how it was quoted, and which verb introduced it.</summary>
        </member>
        <member name="M:DeserokUtils.Features.IfMouseover.ActionLookup.ActionSpan.#ctor(System.String,System.Int32,System.Int32,System.Boolean,System.Boolean)">
            <summary>Where the action name sits in the line, how it was quoted, and which verb introduced it.</summary>
        </member>
        <member name="M:DeserokUtils.Features.IfMouseover.ActionLookup.ActionNameIn(System.String)">
             <summary>
             Pull the action name out of a macro line like <c>/ac "Nascent Flash" {mo}</c>.
            
             ⚠ Returns null rather than guessing when the line is not an action command. The caller then
             degrades to a presence check and SAYS so -- a wrong action id would validate against the wrong
             action's rules, which fails in the direction of "it silently targeted the wrong way".
            
             ⚠⚠ THE SPAN IS RETURNED BECAUSE THIS PARSER IS MORE FORGIVING THAN THE GAME. It happily reads
             an unquoted multi-word name -- `/ac Heart of Corundum {mo}` -- and resolves it, but `/ac`
             itself REQUIRES quotes there. So without the caller re-quoting, everything would work
             perfectly except the last step: a confident, correct decision in the log, followed by nothing
             happening in the game. That is a worse failure than not parsing it at all, because the
             diagnostics would look healthy.
             </summary>
        </member>
        <member name="T:DeserokUtils.Features.IfMouseover.IfMouseoverFeature">
             <summary>
             Use the mouseover if it would actually work, otherwise behave normally -- in ONE line.
            
               /ifmo /ac Clemency {mo}
            
             <c>{mo}</c> becomes <c>&lt;mo&gt;</c> when the action would really land on what you are pointing
             at, and becomes NOTHING otherwise, leaving <c>/ac Clemency</c> -- ordinary targeting.
            
             ## ⚠⚠ What this replaces, and why one line beats thirteen
            
             The vanilla way is a ghetto queue with a fallback at the bottom:
             <code>
             /merror off
             /ac Clemency &lt;mo&gt;      x12
             /ac Clemency             &lt;- the fallback
             </code>
             It works, and it has a race. Press it while the GCD is still rolling and all twelve mouseover
             lines fail on cooldown rather than on targeting; then the GCD expires and the NEXT line to run is
             the bare one. It lands on your target or on you while you had a perfectly good mouseover.
            
             ⭐ The fallback sits last, so it is the line best placed to win exactly when you pressed early --
             which is the whole reason for ghetto-queueing in the first place. One line removes the race
             completely: there is a single attempt and its targeting was decided before it fired.
            
             ## ⚠⚠ Why "is there a mouseover" is the WRONG test
            
             That is what TinyCommands' `-o` checks, and using it here would be WORSE than the vanilla macro.
             Mouse over an enemy while trying to heal: vanilla fires &lt;mo&gt;, fails on an invalid target,
             keeps going, and the bare line rescues it with normal behaviour. A presence check would substitute
             &lt;mo&gt;, fail identically, and have no second line to fall through to. Same for out of range and
             out of line of sight -- every case where vanilla currently degrades correctly.
            
             ⭐ So the question is "would this action work on that target", and the game answers it itself:
             <c>ActionManager.CanUseActionOnTarget</c>.
            
             ⚠ Braces, not angle brackets, for the same reason as CastWatch's {who}: &lt;mo&gt; is the game's
             own syntax and a token the chat pipeline tries to parse is a token that breaks unreadably.
             </summary>
        </member>
        <member name="F:DeserokUtils.Features.IfMouseover.IfMouseoverFeature.lastDecision">
            <summary>Last decision, for the tab. Purely diagnostic.</summary>
        </member>
        <member name="F:DeserokUtils.Features.IfMouseover.IfMouseoverFeature.ambiguityLogged">
             <summary>
             Which PvP/non-PvP resolutions have already been reported, so the line is written once per
             answer instead of once per press.
            
             ⭐ Naturally bounded and so deliberately never swept: the key is a contested action name plus a
             bool, and only 103 names are contested at all. The ceiling is ~206 short strings even if you
             macro every one of them, which is not worth a sweep -- unlike EmoteQuiet's per-sender map,
             where the key space is "every player you ever see".
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.IfMouseover.IfMouseoverFeature.AddMissingQuotes(System.String,System.Int32)">
             <summary>
             Quote a multi-word action name the user forgot to quote.
            
             ⚠⚠ Because forgetting is normal and the failure is invisible. `/ac` requires quotes for
             multi-word names, but this plugin's parser does not -- so an unquoted "Heart of Corundum"
             resolves, validates, picks the right target, and then emits a line the game silently rejects.
             Correct diagnostics, nothing happening. The plugin already knows the real name, so it can just
             put the quotes in.
            
             ⭐ Only ever ADDS quotes around a name that resolved to a real player action, and only when it
             actually contains a space. It never rewrites anything else in your line.
            
             ⚠ Skips the edit if the action name sits AFTER the token, since the index arithmetic would be
             wrong -- nobody writes that, and quietly corrupting a line beats nothing at all.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.IfMouseover.IfMouseoverFeature.Decide(System.String,System.String[])">
             <summary>
             Walk the chain and pick the first placeholder the action would actually land on.
            
             ⭐⭐ Better than the vanilla fallback chain, not merely shorter. Vanilla FIRES AND FAILS down
             the list -- a line per miss, each burning a chance for the GCD to expire onto the wrong one.
             This validates every candidate before anything is sent, so there is one attempt and it is the
             right one.
            
             ⚠⚠ The tail is EXPLICIT, because the correct tail differs per action and no default is right
             for all of them:
             <code>
             {mo}          mouseover, else ordinary targeting      Clemency
             {mo|2}        mouseover, then &lt;2&gt;, else ordinary targeting
             {mo|2|noop}   mouseover, then &lt;2&gt;, else SEND NOTHING     Heart of Corundum
             </code>
             Cover cannot target you, so ordinary targeting merely no-ops there. **Heart of Corundum CAN**,
             so the same fallthrough silently self-casts a cooldown you pressed for somebody else -- and
             deserok has self-use on a separate key. `noop` is how you say "if neither works, do nothing".
            
             ⚠ Segment names are NOT validated against a list. Whatever the game resolves is legal --
             <c>mo</c>, <c>2</c>, <c>t</c>, <c>f</c>, <c>me</c>, and anything else that exists. Same reason
             the mouseover bug happened: an allowlist cannot contain what nobody thought to name.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.IfMouseover.IfMouseoverFeature.Resolve(System.String)">
             <summary>
             Resolve one chain segment through the game's own placeholder resolver.
            
             ⚠ Dalamud's world-only mouseover reading is logged alongside for <c>mo</c>, because that
             disagreement is exactly the bug that shipped in 1.7.0 and it should stay visible.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.IfMouseover.IfMouseoverFeature.Reason(System.UInt32)">
             <summary>
             The game's own words for a status code, since the code IS a LogMessage row id.
            
             ⭐ Reading it back out of the sheet rather than hardcoding "566 means out of range". The
             mapping came from the game and stays coming from the game, so a patch that renumbers these
             makes the log read oddly instead of making it read confidently wrong.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.IfMouseover.IfMouseoverFeature.Trace(System.String)">
             <summary>
             Every decision, to BOTH the log and the diagnostic channel.
            
             ⚠⚠ THE SAME GAP AS DRAWSHEATHE, MADE TWICE IN ONE NIGHT. Diag is off by default, so the first
             /ifmo test produced a dalamud.log containing the action-map line and nothing else -- the
             command had clearly run and left no evidence of what it decided. A macro press is a rare
             event; one line each costs nothing against being able to read what happened afterwards
             without asking for a toggle and a repeat.
             </summary>
        </member>
        <member name="T:DeserokUtils.Features.Interact.InteractFeature">
             <summary>
             A key that operates the thing in front of you and NOTHING else.
            
               /dsuinteract
            
             ## The problem
            
             Num0 (Confirm) is console-shaped and serves two masters: it operates world objects AND drives
             menus. It also takes two presses for a lever -- one to target, one to use -- so you press it
             repeatedly. **With any menu open those presses land in the menu**, drop a cursor on it, and
             activate whatever is under it.
            
             deserok keeps consumables at the top of his inventory for quick access, which turns a Watcher's
             Tower key-and-wheel run with the bags open into a raid-grade strength potion consumed during a
             levelling roulette. Everything else in this plugin fails by doing nothing; this fails by
             spending something.
            
             ⭐ A dedicated key cannot have that failure, because it is not the Confirm key -- there is no
             menu path for it to be routed down.
            
             ## ⭐ The guard is deserok's, and it is better than what was proposed
            
             A quiet-gap collapse was suggested, copying DrawSheathe. His answer was to *"lock out if casting
             anything, since interacting with everything is always a cast bar"* -- which is the game's own
             state rather than an interval somebody chose, and therefore correct at every animation length
             instead of at the one that got measured. Same reason SheatheCooldown beat an invented number.
             </summary>
        </member>
        <member name="F:DeserokUtils.Features.Interact.InteractFeature.Floor">
            <summary>
            ⚠ A floor for interactions with NO cast bar. deserok: *"maybe a 1 second lockout, some things
            don't have a castbar (aetheryte does not)"* -- so IsCasting alone has a hole, and this covers
            it. Two conditions, but they guard genuinely different things rather than duplicating.
            </summary>
        </member>
        <member name="M:DeserokUtils.Features.Interact.InteractFeature.DoInteract">
             <summary>
             Operate the thing in front of you, and touch nothing else.
            
             ⭐ Calls <c>InteractWithObject(obj, checkLineOfSight: true)</c> -- the path the recording showed
             Num0 taking. The mouse-click path pairs it with OpenObjectInteraction and passes
             <c>false</c>; that one is the menu-opening route and is deliberately NOT what this uses.
             </summary>
        </member>
        <member name="M:DeserokUtils.Features.Interact.InteractFeature.Choose(Dalamud.Game.ClientState.Objects.Types.IGameObject)">
             <summary>
             Which object to operate, and how it was chosen.
            
             ⭐ The first two rules ask the GAME what it has already picked -- soft target, then hard target
             -- rather than re-deriving it. Only the last resort scans, and that scan is the one place a
             kind list appears.
            
             ⚠⚠ That list is the allowlist shape that has bitten this project twice, so it is arranged to
             FAIL LOUDLY: anything nearby that is rejected gets logged, so a missing kind shows up as a
             named candidate rather than as "the key does nothing near that thing". Kinds seen in the
             recording were EventObj, Treasure and Aetheryte; the rest are added on the same evidence or
             not at all.
             </summary>
        </member>
        <member name="F:DeserokUtils.Features.Interact.InteractFeature.Reach">
            <summary>Interaction reach in yalms. Generous -- the guard against reaching too far is the
            game refusing, not this number.</summary>
        </member>
        <member name="M:DeserokUtils.Features.Interact.InteractFeature.Section(System.String)">
            <summary>ImGui.SeparatorText does not exist in this binding version; this is the stand-in.</summary>
        </member>
        <member name="T:DeserokUtils.Features.Interact.InteractSniffer">
             <summary>
             Watch what the Confirm key actually calls when you operate something in the world.
            
             ## ⚠⚠ The problem this exists to solve is DESTRUCTIVE, not cosmetic
            
             Num0 (Confirm) is console-shaped: it targets a lever on the first press and uses it on the
             second, so you naturally press it repeatedly. **With any menu open, those presses land in the
             menu instead** -- a cursor appears and activates whatever is under it. deserok keeps consumables
             at the top of his inventory for quick access, so a Watcher's Tower key-and-wheel run with the
             inventory open has eaten a raid-grade strength potion in a levelling roulette.
            
             Everything else in this plugin fails by doing nothing. This one SPENDS something.
            
             ⭐ A dedicated key cannot have that failure, because it is not the Confirm key. It never confirms
             a dialogue, never advances text, never lands a cursor in a menu -- there is nothing to route.
            
             ## What the recording has to answer
            
             The game has TWO functions here and they are not the same thing:
             <code>
             TargetSystem.InteractWithObject(GameObject*, bool checkLineOfSight)
             TargetSystem.OpenObjectInteraction(GameObject*)
             </code>
             Which one does Confirm use? Does the first press (target) differ from the second (use)? And --
             the part that decides the whole design -- **what object does it pass**, so a command can pick the
             same one instead of choosing between current target, soft target and nearest-in-range on a guess.
            
             ⚠ Armed on demand. These are cold, but a diagnostic that installs itself at load is how the
             per-frame audit happened.
             </summary>
        </member>
        <member name="T:DeserokUtils.Features.Interact.InteractSniffer.InteractDelegate">
            <summary>Signatures verbatim from FFXIVClientStructs, not from memory.</summary>
        </member>
        <member name="T:DeserokUtils.Features.Interact.InteractSniffer.OpenDelegate">
            <summary>TargetSystem.OpenObjectInteraction(GameObject*)</summary>
        </member>
        <member name="F:DeserokUtils.Features.Interact.InteractSniffer.DefaultDuration">
            <summary>Long by default -- a dungeon pull is minutes, not seconds.</summary>
        </member>
        <member name="M:DeserokUtils.Features.Interact.InteractSniffer.Describe(FFXIVClientStructs.FFXIV.Client.Game.Object.GameObject*)">
            <summary>
            ⚠ Reports the OBJECT KIND and the data id as well as the name. The name alone will not
            distinguish "the lever" from "a lever" across a dungeon, and the design question is which
            class of thing a command should look for -- not which one deserok happened to stand next to.
            </summary>
        </member>
        <member name="T:DeserokUtils.GameCommands">
             <summary>
             Two ways to run a text command, and WHICH ONE YOU PICK IS LOAD-BEARING.
            
             ⚠⚠ Measured 2026-08-02, and it is the bug that made CastWatch look broken for a day: the queued
             path is what every guide suggests and what TinyCommands uses, but it QUEUES. A /macrocancel sent
             that way was still sitting in the queue when the macro executed its next line, so the line it
             was meant to suppress went out anyway. The gate was correct; the suppression lost the race.
             </summary>
        </member>
        <member name="M:DeserokUtils.GameCommands.RunNow(System.String)">
            <summary>
            Runs the command IMMEDIATELY, in-line, before the macro can advance.
            ✅ Use for anything that must beat the next macro line -- /macrocancel above all.
            ⚠ Bypasses the chatbox pipeline, so placeholders like &lt;t&gt; are NOT expanded.
            </summary>
        </member>
        <member name="M:DeserokUtils.GameCommands.Queue(System.String)">
            <summary>
            Queues the command through the chatbox, exactly as if typed.
            ✅ Use for anything the USER wrote: this is the path that expands &lt;t&gt;, &lt;mo&gt;,
            &lt;2&gt; and friends, and its timing does not matter when nothing has to be outrun.
            </summary>
        </member>
        <member name="T:DeserokUtils.Plugin">
            <summary>
            A bucket for small utilities. This file is the SHELL only -- services, the window, and the
            plugin-level command. Every feature owns a folder under Features/, registers its own commands,
            and contributes one tab. Nothing feature-specific belongs here, so adding the second utility
            never means editing the first one's code.
            </summary>
        </member>
        <member name="P:DeserokUtils.Plugin.Verbose">
             <summary>
             Diagnostic output to the Debug chat channel. Shared across features, because "what did it
             see, and why did it decide that" is the same question every time.
            
             ⚠⚠ A PASSTHROUGH to the config, not a separate field. It used to be its own static seeded
             from the config at load, so both the checkbox and /dsu debug flipped the static and never
             saved -- turn it off, restart, and it was on again, with nothing anywhere explaining why.
             </summary>
        </member>
        <member name="P:DeserokUtils.Plugin.OpenWindow">
            <summary>Static hook so a feature can open the window without holding a reference to it.</summary>
        </member>
        <member name="M:DeserokUtils.Plugin.Diag(System.String)">
            <summary>
            Diagnostics go to the Debug channel so they land in a tab configured for them rather than in
            the middle of a pull. Genuine user-facing errors do NOT come through here -- those stay in
            normal chat where they cannot be missed.
            </summary>
        </member>
        <member name="M:DeserokUtils.Plugin.Announce(System.String)">
             <summary>
             A thing the user asked to be told about -- NOT a diagnostic.
            
             ⚠ Deliberately separate from Diag: diagnostics go to a tab you check afterwards, an alert has
             to reach you while you are looking at the game. Routing both to the same place would mean
             either the alerts get buried or the diagnostics start interrupting.
             </summary>
        </member>
        <member name="T:DeserokUtils.UI.TabEntry">
             <summary>
             What a feature tells the window about itself.
            
             ⚠ A DESCRIPTOR, not an interface. The temptation with five features is to declare IFeature and
             have everything implement it -- and the feature shapes still refuse to agree (a hook, a poller, a
             step machine, a bare command handler, a chat filter). What they DO agree on is how they present,
             so that is the only thing given a type. See DeserokUtils.md on why the interface keeps not
             happening.
            
             ⭐ <see cref="P:DeserokUtils.UI.TabEntry.Summary"/> is the future-proofing, and it costs one string. SimpleTweaks' entire UI
             -- categories, collapsed rows, and the search box that makes a hundred tweaks usable -- is built
             on every tweak having a name and a one-line description. Having that field from the start means
             search is an addition later rather than a refactor. Nothing reads it yet except grouped tabs.
             </summary>
        </member>
        <member name="M:DeserokUtils.UI.TabEntry.#ctor(System.String,System.String,System.String,System.Action)">
             <summary>
             What a feature tells the window about itself.
            
             ⚠ A DESCRIPTOR, not an interface. The temptation with five features is to declare IFeature and
             have everything implement it -- and the feature shapes still refuse to agree (a hook, a poller, a
             step machine, a bare command handler, a chat filter). What they DO agree on is how they present,
             so that is the only thing given a type. See DeserokUtils.md on why the interface keeps not
             happening.
            
             ⭐ <see cref="P:DeserokUtils.UI.TabEntry.Summary"/> is the future-proofing, and it costs one string. SimpleTweaks' entire UI
             -- categories, collapsed rows, and the search box that makes a hundred tweaks usable -- is built
             on every tweak having a name and a one-line description. Having that field from the start means
             search is an addition later rather than a refactor. Nothing reads it yet except grouped tabs.
             </summary>
        </member>
        <member name="T:DeserokUtils.UI.MainWindow">
             <summary>
             One tab per feature, or one tab per GROUP of related features.
            
             The window still knows nothing about any feature -- it is handed descriptors, so a new utility
             never edits this file.
            
             ⭐ Grouping exists because the macro tools crossed the threshold: CastWatch and /ifmo are two
             faces of "FFXIV macros are bad". Five top-level tabs was fine; what starts a reader hunting is
             near-duplicates sitting apart from each other in a longer list. Anything ungrouped stays a
             plain top-level tab, which is every other feature -- grouping is earned by having relatives, not
             applied on principle.
             </summary>
        </member>
        <member name="M:DeserokUtils.UI.MainWindow.DrawGroupedTab(System.String,System.Collections.Generic.IReadOnlyList{DeserokUtils.UI.TabEntry})">
             <summary>
             Related features as collapsible sections, the shape SimpleTweaks uses.
            
             ⭐ Collapsed by DEFAULT, and that is right rather than merely tidy: most of what these
             sections contain is reference -- command tables, token explanations, macro templates -- read
             once and then in the way. An expander turns permanent clutter into something you open when you
             have forgotten the syntax.
            
             ⚠ Nested tab bars were the other option and were not taken. Tabs hide their siblings; headers
             let you see everything the group contains at a glance, which is what makes the grouping worth
             having in the first place.
             </summary>
        </member>
    </members>
</doc>
